# Caddy Manager Pro — 실행보고서

> 최종 업데이트: 2026년 4월 9일 (62회차)  
> 프레임워크: Next.js 14 + TypeScript + Tailwind CSS  
> 서버: http://localhost:4455  
> DB: Supabase (lajjbrrysvkaxzrchanp)  
> 스토리지: Cloudflare R2 (aonepro-db 버킷)  
> 결제: PortOne V2
> **SMS: 솔라피 (Solapi) — 알리고 → 솔라피로 교체 (IP 제한 없음)**

---

## ✅ 57회차 — 스마트폰 캘린더 구독 URL 동기화(ICS) 구현 + 프리미엄 업셀 UI

### 구현 목적
- 캐디 매니저 Pro 일정을 스마트폰 기본 캘린더(iOS/Android)와 동기화
- 외부 유료 API 없이 `ICS` 구독 URL 방식으로 무료 제공
- 스탠다드/프리미엄 티어 차등 제공으로 업셀 유도

### 핵심 동작
- 신규 API: `GET /api/calendar/[code].ics`
- 이용권 코드 유효성/활성/만료 상태 확인 후 일정 조회
- 티어 분기:
  - `standard`: 근무(`work`) 일정만 캘린더 제공
  - `premium`: 근무/개인/휴무 전체 일정 제공
- 설정 화면에 `URL 복사` + `등록 방법 보기` + `프리미엄 전환 CTA` 추가
- 설정 상단에 `스마트폰 캘린더 자동 동기화` 강조 배너 + `바로 열기` 앵커 버튼 추가
- 등록 방법 모달에 `삼성 갤럭시 기본 캘린더` 설치 절차(Google 캘린더 경유 권장) 추가

### 생성/수정 파일
- `src/app/api/calendar/[code]/route.ts` — ICS 캘린더 동적 생성 API (신규)
- `src/app/home/page.tsx` — 캘린더 동기화 카드를 설정 화면으로 이동, 설정 이동 버튼 추가
- `src/app/settings/page.tsx` — 이용권 정보 티어 표시 + 캘린더 동기화 UI를 상단 강조형으로 개선
- `src/app/api/notify/settings/route.ts` — Supabase env 누락 시 런타임 예외 방지 처리
- `src/app/settings/page.tsx` — 캘린더 등록 방법에 삼성 갤럭시 기본 캘린더 절차 추가
- `실행보고서.md` — 57회차 반영

### 환경변수/스키마 변경
- 환경변수 변경 없음
- Supabase 스키마 변경 없음

### 검증 결과 (로컬 스모크 테스트)
- `npm run build` 성공 (Next.js 14.2.35)
- `http://localhost:4455/api/calendar/INVALID-CODE.ics` → `404` 확인
- `http://localhost:4455/api/calendar/DC-AWA-S72.ics` → `403` 확인 (만료/비활성 이용권 차단 정상 동작)

### 미완료/다음 작업
- 실기기 등록 테스트: iPhone 구독 캘린더 / Google Calendar URL 구독
- 필요 시 구독 URL 토큰화(이용권 코드 직접 노출 완화)

---

## ✅ 58회차 — C안 캘린더 자동동기화 구현계획 HTML 문서 + 미리보기 이미지 제작

### 구현 목적
- C안(사용자 거의 무조작) 실행 방향을 한 번에 공유 가능한 기획 문서로 정리
- 개발/운영/업셀 포인트를 시각적으로 보여주는 미리보기 이미지 첨부

### 핵심 구성
- 신규 HTML 문서 작성: 구현 범위, 단계별 일정, KPI, 확장(C안) 방향
- 문서 내 `img` 태그로 미리보기 이미지(SVG) 직접 첨부
- 모바일 화면에서도 읽기 쉬운 반응형 레이아웃 적용

### 생성/수정 파일
- `html/C안_캘린더자동동기화_구현계획.html` — C안 실행 계획 본문 문서 (신규)
- `html/C안_캘린더자동동기화_구현계획_미리보기.svg` — 진행 미리보기 이미지 (신규)
- `실행보고서.md` — 58회차 반영

### 환경변수/스키마 변경
- 환경변수 변경 없음
- Supabase 스키마 변경 없음

### 미완료/다음 작업
- 필요 시 동일 콘텐츠를 `html/실행보고서.html`에도 동기 반영
- 사용자 검토 후 문구/색상 톤 보정 및 배포 여부 결정

---

## ✅ 59회차 — HTML 실행보고서 동기 반영 (58회차 시각 섹션 추가)

### 구현 목적
- 마크다운 보고서와 HTML 보고서 간 최신 회차 정보 일치
- 공유 시 바로 열람 가능한 C안 문서/미리보기 링크를 실행보고서 HTML에 직접 제공

### 핵심 구성
- `html/실행보고서.html` 헤더 작성일 최신화 (`2026-04-09`)
- 헤더에 `최신 회차: 58회차` 메타 카드 추가
- 58회차 섹션을 상단에 신설하고, 아래 항목을 포함:
  - 작업 목적/구성 요약 테이블
  - 생성 파일 체크리스트
  - 구현계획/미리보기 바로가기 버튼
  - 미리보기 이미지 본문 임베드

### 생성/수정 파일
- `html/실행보고서.html` — 58회차 시각 섹션 동기 반영
- `실행보고서.md` — 59회차 반영

### 환경변수/스키마 변경
- 환경변수 변경 없음
- Supabase 스키마 변경 없음

### 미완료/다음 작업
- 필요 시 실행보고서 HTML 전체를 최신 회차 기준으로 재정렬/목차화
- 사용자 요청 시 문서형(투자제안형/영업형) 2종 추가 제작

---

## ✅ 60회차 — C안 실제 구현 완료 (캘린더 등록 시작 페이지 + 기기별 원클릭 동선)

### 구현 목적
- 단순 URL 복사 단계에서 끝나지 않고, 실제 등록 완료까지 이어지는 C안 흐름 구현
- iPhone/Android/데스크톱 각각에서 가장 짧은 동선으로 캘린더 구독을 시작하도록 개선

### 핵심 구성
- 신규 라우트: `/calendar-sync`
- 기기 감지 후 빠른 실행 버튼 분기
  - `iPhone`: `webcal://` 기반 캘린더 바로 열기
  - `Android`: Google Calendar `add by URL` 화면으로 즉시 이동
  - `Desktop`: QR 스캔 중심 안내
- 보조 수단 추가
  - 구독 URL 복사
  - Web Share API 기반 링크 공유
  - QR 코드 표시
- 설정 화면의 캘린더 섹션을 `등록 시작` 중심 흐름으로 개편

### 생성/수정 파일
- `src/app/calendar-sync/page.tsx` — 캘린더 자동 동기화 전용 시작 페이지 (신규)
- `src/app/settings/page.tsx` — `등록 시작` / `URL 복사` / `링크 공유` 흐름으로 개선
- `package.json` — `react-qr-code` 의존성 추가
- `package-lock.json` — 패키지 설치 반영
- `실행보고서.md` — 60회차 반영

### 환경변수/스키마 변경
- 환경변수 변경 없음
- Supabase 스키마 변경 없음

### 검증 결과
- `npm install react-qr-code` 성공
- `npm run build` 성공
- 내부서버 재시작 완료
- `http://localhost:4455/home/` → `200`
- `http://localhost:4455/settings/` → `200`
- `http://localhost:4455/calendar-sync/` → `200`

### 미완료/다음 작업
- 실제 iPhone Safari `webcal://` 동작과 삼성/Google 계정별 반영 속도 실기기 점검
- 필요 시 `구독 완료 체크` 저장 및 재안내 UX 추가

---

## ✅ 61회차 — 스마트폰 전용 정책 반영 + 스탠다드 동기화 완전 차단

### 구현 목적
- "100% 스마트폰 설정" 운영 방향으로 동선을 단순화
- 스탠다드 요금제에서 캘린더 동기화 버튼 자체를 비활성화

### 핵심 구성
- 설정 화면에서 스탠다드일 때 캘린더 동기화 버튼 비활성(등록 시작/URL 복사/링크 공유 잠금)
- 캘린더 API를 프리미엄 전용으로 강제 (`standard` 요청 시 `403`)
- `/calendar-sync` 페이지에서 데스크톱 전송/QR 섹션 제거
- `/calendar-sync`는 프리미엄 + 스마트폰에서만 실사용 흐름 제공

### 생성/수정 파일
- `src/app/settings/page.tsx` — 스탠다드 버튼 잠금 및 안내 문구 반영
- `src/app/api/calendar/[code]/route.ts` — 프리미엄 전용 서버 차단 로직 추가
- `src/app/calendar-sync/page.tsx` — QR/데스크톱 전송 제거, 스마트폰 전용 분기
- `package.json` / `package-lock.json` — `react-qr-code` 제거
- `실행보고서.md` — 61회차 반영

### 환경변수/스키마 변경
- 환경변수 변경 없음
- Supabase 스키마 변경 없음

### 검증 결과
- `npm run build` 성공
- 구조 변경 후 내부서버 재시작 완료
- `http://localhost:4455/home/` → `200`
- `http://localhost:4455/settings/` → `200`
- `http://localhost:4455/calendar-sync/` → `200`

### 미완료/다음 작업
- 스마트폰 실기기에서 스탠다드/프리미엄 계정 각각 버튼 잠금/활성 최종 확인
- 배포 시 실사용 링크 기준으로 재검수

---

## ✅ 62회차 — 관리자 등급관리 탭 구현 (업그레이드/다운그레이드)

### 구현 목적
- 관리자 페이지에서 이용권 등급을 즉시 변경할 수 있도록 전용 관리 탭 제공
- 운영자가 DB 직접 수정 없이 UI에서 `standard/premium` 업·다운 처리

### 핵심 구성
- 관리자 탭 추가: `등급관리`
- 검색(이름/전화번호/코드) 후 대상 이용권 목록 확인
- 이용권별 현재 등급 뱃지 표시 + 버튼 제공
  - `업그레이드` (standard → premium)
  - `다운그레이드` (premium → standard)
- 변경 전 확인 모달 + 변경 중 상태 표시 + 완료 알림

### 생성/수정 파일
- `src/app/admin/page.tsx` — `등급관리` 탭 및 등급 변경 액션 추가
- `실행보고서.md` — 62회차 반영

### 환경변수/스키마 변경
- 환경변수 변경 없음
- Supabase 스키마 변경 없음

### 검증 결과
- `npm run build` 성공
- 구조 변경 후 내부서버 재시작 완료
- `http://localhost:4455/home/` → `200`
- `http://localhost:4455/settings/` → `200`
- `http://localhost:4455/admin/` → `200`

### 미완료/다음 작업
- 관리자 계정으로 실제 이용권 1건 업/다운 동작 확인
- 필요 시 변경 사유/변경자/변경시각 감사로그 테이블 연동

---

## ✅ 1회차 — PortOne V2 결제 연동

| 항목 | 내용 |
|------|------|
| storeId | `store-17246cf6-fccb-48c5-b664-3e15304c7ea1` |
| channelKey | `channel-key-5e47b520-75c7-4963-9e13-adf55d54ede7` |
| API Secret | `.env.local` 등록 완료 |
| SDK | `layout.tsx`에 PortOne V2 CDN 스크립트 추가 |

### 생성/수정 파일
- `src/lib/paymentService.ts` — PortOne V2 결제 요청 헬퍼
- `src/app/subscribe/page.tsx` — 고객 결제 페이지 (3가지 요금제)
- `src/app/api/payment/complete/route.ts` — 서버사이드 결제 검증 + 이용권 코드 발급
- `src/app/layout.tsx` — SDK Script 추가

### 요금제
| 플랜 | 금액 | 기간 |
|------|------|------|
| 1개월 | 9,900원 | 30일 |
| 6개월 | 55,000원 | 180일 |
| 1년 | 99,000원 | 365일 |

---

## ✅ 2회차 — R2 클라우드 백업 구현

### 백업 정책
| 역할 | 동작 |
|------|------|
| 자동 백업 | 앱 실행 시 하루 1회 자동 R2 전송 |
| 수동 백업 | "백업 (파일 저장 + 클라우드)" 버튼 → 파일 저장 + R2 동시 |
| 복원 | **관리자 전용** — 사용자 화면에서 복원 버튼 제거 |

### 저장 경로
```
caddy-manager-pro/backups/{이용권코드}/latest.json
caddy-manager-pro/backups/{이용권코드}/YYYY-MM-DD.json
```

### 생성/수정 파일
- `src/lib/r2Client.ts` — uploadBackup / downloadBackup
- `src/app/api/backup/upload/route.ts`
- `src/app/api/backup/download/route.ts`
- `src/app/settings/page.tsx` — 복원 버튼 제거, 수동 백업 버튼 통합
- `src/components/layout/ClientLayout.tsx` — `runAutoBackupIfNeeded()` 추가

---

## ✅ 3회차 — 관리자 페이지 개편

### 탭 구조 (6탭)
| 탭 | 기능 |
|------|------|
| 코드발급 | 채널·요금제·일수 선택 후 이용권 코드 발급 |
| 이용내역 | 전체 고객 코드/만료일/상태 조회 |
| 딜러관리 | 딜러 등록/비활성/URL 복사 |
| 정산관리 | 딜러별 미정산·정산완료 내역 |
| 캐디관리 | 월간 휴무표 / 예비자 명단 / 기본 순번 |
| **데이터복구** | 이용권 코드 → R2 백업 조회 → JSON 파일 다운로드 |

### 데이터복구 탭 흐름
```
이용권 코드 입력 → [백업 조회] → 날짜/크기 표시 → [JSON 다운로드] → 고객 전달
```

### 관리자 인증
- 비밀번호: `0827`
- 5회 오류 → 30분 잠금

---

## ✅ 4회차 — 딜러 대시보드 구현

### 신규 라우트
```
/dealer/[token]             — 기존 현장 발급 전용 페이지
/dealer/[token]/dashboard   — 딜러 전용 대시보드 (신규)
```

### 딜러 대시보드 탭 구조
| 탭 | 기능 |
|------|------|
| 코드발급 | 고객 정보 입력 후 이용권 발급 |
| 내 수익 | 이번달/누적/미정산/정산완료 금액 + 상세 내역 |
| 내 고객 | 본인이 발급한 고객 목록 (상태/만료일/연락처) |
| 정산요청 | 미정산 금액 확인 + 관리자에게 정산 요청 |

### PIN 보안
- 딜러 등록 시 4~8자리 숫자 PIN 설정 (선택)
- PIN 없으면 바로 진입
- PIN 3회 오류 → 30분 잠금

### 관리자 딜러관리 탭 업데이트
- PIN 입력 필드 추가
- 대시보드 URL 복사 버튼 별도 추가 (발급URL / 대시보드 2개)
- 딜러 카드에 PIN 번호 표시

### Supabase 컬럼 추가
```sql
ALTER TABLE aone_pro_caddypro_dealers ADD COLUMN IF NOT EXISTS pin TEXT;
```

---

## ✅ 5회차 — 딜러 등록 폼 + 랜딩 페이지

### 딜러 등록 폼 개선
- 전화번호 자동 하이픈 포맷 (`010-0000-0000`)
- 이메일 입력란 추가
- `@` 또는 `.` 누락 시 팝업 모달 알림 (alert 대신 커스텀 모달)

### Supabase 컬럼 추가
```sql
ALTER TABLE aone_pro_caddypro_dealers ADD COLUMN IF NOT EXISTS email TEXT;
```

### 랜딩 페이지 (`/landing`)
- 다크 테마 모바일 최적화 랜딩 페이지
- 요금제 3가지 선택 UI
- [구매하기] → `/subscribe?plan=xxx` 연결
- [이용권 코드가 있으신가요?] → 바텀시트 코드 입력
- 코드 입력 후 메인 이동 시 LicenseGuard에서 **자동 적용**
- LicenseGuard 우회 처리 완료

---

## 📁 주요 파일 목록

```
src/
├── app/
│   ├── admin/page.tsx              — 관리자 도구 (6탭)
│   ├── dealer/[token]/
│   │   ├── page.tsx                — 현장 발급 페이지
│   │   └── dashboard/page.tsx      — 딜러 대시보드 (신규)
│   ├── landing/page.tsx            — 랜딩/세일즈 페이지 (신규)
│   ├── subscribe/page.tsx          — 결제 페이지
│   ├── settings/page.tsx           — 설정 + 수동 백업
│   └── api/
│       ├── backup/upload/route.ts
│       ├── backup/download/route.ts
│       └── payment/complete/route.ts
├── lib/
│   ├── paymentService.ts
│   ├── r2Client.ts
│   ├── licenseUtils.ts
│   └── supabaseClient.ts
└── components/
    └── layout/
        ├── ClientLayout.tsx        — 자동백업 + 라우트 제어
        └── LicenseGuard.tsx        — 라이선스 인증 + pending_code 처리
```

---

## 📌 환경변수 (.env.local)

```
NEXT_PUBLIC_SUPABASE_URL=https://lajjbrrysvkaxzrchanp.supabase.co
NEXT_PUBLIC_PORTONE_STORE_ID=store-17246cf6-...
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=channel-key-5e47b520-...
PORTONE_SECRET=DYAyDO6H...
R2_ACCOUNT_ID=8e9d3fd6...
R2_ACCESS_KEY_ID=d8e65676...
R2_SECRET_ACCESS_KEY=df1798c4...
R2_BUCKET_NAME=aonepro-db
R2_ENDPOINT=https://8e9d3fd6...r2.cloudflarestorage.com
NEXT_PUBLIC_R2_PUBLIC_URL=https://pub-7c8896c3...r2.dev
```

---

## 🔐 인증 정보

| 항목 | 값 |
|------|------|
| 관리자 비밀번호 | `0827` |
| 마스터코드 | `0827` |
| 이용권 코드 형식 | `SM-XXX-XXX` |

---

---

## ✅ 6회차 — 버그수정 + GitHub 신규 저장소 + Vercel 배포 시도

### 완료 작업
- `supabaseClient.ts` — 빌드타임 즉시초기화 → Proxy 지연초기화로 수정 (Vercel 빌드오류 fix)
- `admin/page.tsx` — `handleAddDealer` try/finally 추가 (딜러등록 무한루프 버그 수정)
- `.github/copilot-instructions.md` — 실행보고서 항상 업데이트 규칙 추가
- 신규 GitHub 저장소 생성: `https://github.com/jslala222/caddy_pro_enterprise`
- `dealer/[token]/page.tsx` + `dashboard/page.tsx` — 고객 전화번호 자동 하이픈 포맷
- `dealer-login/page.tsx` — 딜러 전용 로그인 페이지 신규 생성 (전화번호+PIN → 대시보드)
- `settings/page.tsx` — 하단에 "딜러 로그인" 버튼 추가
- `ClientLayout.tsx` — `/dealer-login` 라우트 LicenseGuard 우회 추가

### 수정 파일
- `src/lib/supabaseClient.ts`
- `src/app/admin/page.tsx`
- `.github/copilot-instructions.md`

### Vercel 배포 상태
- 빌드 오류 (supabaseUrl 미설정) → 코드 수정 완료
- 환경변수 Vercel에 추가 필요 (Settings → Environment Variables)
- 수정 코드 push 완료 (commit: 53b25c0)

### Vercel 설정 필요 환경변수
```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
NEXT_PUBLIC_PORTONE_STORE_ID
NEXT_PUBLIC_PORTONE_CHANNEL_KEY
PORTONE_SECRET
R2_ACCOUNT_ID / R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY
R2_BUCKET_NAME / R2_ENDPOINT / NEXT_PUBLIC_R2_PUBLIC_URL
```

---

## 📋 다음 작업 후보

- [ ] Vercel 환경변수 추가 후 재배포
- [ ] 딜러 등록 로컬 테스트 완료 확인
- [ ] 랜딩 페이지 도메인 연결
- [ ] 딜러 정산 요청 알림 (카카오 알림톡 or 이메일)
- [ ] 이용권 만료 전 자동 알림

---

## ✅ 7회차 — 딜러 결제 링크 공유 기능 구현

### 배경
- 기존: 딜러가 코드 발급 후 화면에서 직접 보고 수동으로 문자 전달 → 누락 위험
- 개선: 딜러가 결제 링크를 생성 → 고객이 직접 카드결제 → 코드 자동 발급

### 구현 내용

#### 딜러 발급 페이지 (`dealer/[token]/page.tsx`)
- "이용권 즉시 발급" 버튼 아래 "또는" 구분선 추가
- **"결제 링크 생성 (고객 직접 카드결제)"** 버튼 추가
- 클릭 시 `/subscribe?name=고객명&phone=번호&plan=플랜&ref=딜러토큰` URL 생성
- 생성된 링크 표시 패널: 링크 미리보기 + "링크 복사" + "공유하기" 버튼
- "공유하기" → `navigator.share` (모바일) or 클립보드 복사 (PC)

#### 딜러 대시보드 (`dealer/[token]/dashboard/page.tsx`)
- 코드발급 탭에 동일하게 결제 링크 생성 기능 추가

#### 결제 페이지 (`subscribe/page.tsx`)
- URL 파라미터 (`name`, `phone`, `plan`, `ref`) 자동으로 폼에 초기화
- `ref` 파라미터 있으면 상단에 **"딜러 추천 링크로 접속하셨습니다"** 안내 배너 표시

### 이용 흐름
```
딜러가 고객 정보 입력
→ "결제 링크 생성" 클릭
→ URL 생성: /subscribe?name=박지수&phone=010-3344-5566&plan=3month&ref=DEALER_TOKEN
→ "공유하기" 또는 "링크 복사" → 카카오/문자로 전달
→ 고객이 링크 클릭 → 이름/번호/플랜 자동 입력된 결제 페이지 열림
→ 고객 결제 완료 → 이용권 코드 자동 발급 표시
```

### 수정 파일
- `src/app/dealer/[token]/page.tsx` — Share2, ExternalLink 아이콘 추가 + 링크 생성 로직
- `src/app/dealer/[token]/dashboard/page.tsx` — 동일 기능 추가
- `src/app/subscribe/page.tsx` — URL 파라미터 자동입력 + 딜러 추천 배너
- `html/demo_issue_flow.html` — 발급 흐름 애니메이션 데모 (신규)

### 빌드 결과
- ✅ 20페이지 빌드 성공, 오류 0개
- ✅ http://localhost:4455 정상 실행

---

## ✅ 8회차 — 딜러 대시보드 고도화 (골프장명/결제방식/수익집계)

### 버그 수정
- **내 고객 탭 빈 화면**: `created_at` → `issued_at` 전면 교체 (컬럼명 불일치)

### DB 스키마 추가
```sql
ALTER TABLE aone_pro_caddypro_licenses
  ADD COLUMN IF NOT EXISTS golf_course TEXT,
  ADD COLUMN IF NOT EXISTS pay_method TEXT DEFAULT 'cash';
```

### 코드발급 탭 UI 개선
- 메모 1개 → **골프장명** + **특이사항** 2개 필드로 분리
- **수금방식 선택** (3가지):
  - **안A** 현금 즉시 발급 (`pay_method = 'cash'`)
  - **안B** 가상계좌 (`pay_method = 'virtual_account'`)
  - **안B-2** 실시간계좌이체 (`pay_method = 'transfer'`)

### 내 고객 탭 상세보기 강화
- 발급일, 상품명(플랜+일수), 골프장명, 특이사항, 수금방식, 만료일 표시

### 내 수익 탭 신규 구성
1. **요약 카드 4개**: 이번달 / 누적 / 미정산 / 정산완료
2. **💵 현금 수금 내역 (본사 정산 대상)**: 현금발급 건별 고객명·플랜·날짜·금액 + 합계
3. **📦 상품별 발급 현황**: 1개월/6개월/1년 별 건수·금액 집계
4. **📅 날짜별 발급 현황**: 일자별 건수·금액 집계 (최신 우선)
5. **정산 내역**: Supabase settlements 테이블 기반 정산 상세

### 수정 파일
- `src/lib/licenseUtils.ts` — `issueVoucher()` 에 `golfCourse`, `payMethod` 파라미터 추가, DB INSERT에 반영
- `src/app/dealer/[token]/dashboard/page.tsx` — License 인터페이스·상태·UI 전면 개선

### 빌드 결과
- ✅ 20페이지 빌드 성공, 오류 0개
- ✅ http://localhost:4455 정상 실행

## 📋 다음 작업 후보

- [ ] subscribe.tsx: `payMethod` URL 파라미터로 가상계좌/실시간이체 결제창 분기
- [ ] Vercel 환경변수 추가 후 재배포
- [ ] 딜러 정산 요청 알림 (카카오 알림톡 or 이메일)
- [ ] 이용권 만료 전 자동 알림

---

## ✅ 10회차 — 만료 고객 재진입 UX (안 B: 만료 모달 + 전화번호 찾기)

### 구현 내용

**`src/lib/licenseUtils.ts`**
- `verifyLicenseAsync` 만료 시 `expiresAt` 필드도 함께 반환하도록 수정

**`src/components/layout/LicenseGuard.tsx`**
- 앱 시작 시 `caddy_expires_at` 로컬 캐시로 만료 감지 (네트워크 없이 즉각 처리)
- 코드 수동 입력 후 만료 → 에러 텍스트 대신 만료 모달로 전환
- **만료 모달**: 코드·만료일 표시 + [온라인 갱신] + [딜러 안내] + [다른 코드 입력]
- **전화번호로 찾기**: 잠금화면 하단에 토글 섹션 추가
  - 전화번호 입력 → `searchLicenseByPhone` 호출
  - 유효한 코드 발견 → 코드 자동 입력 후 활성화
  - 만료된 코드 발견 → 만료 모달로 자동 전환
- `caddy_lookup_mode` localStorage 플래그로 랜딩에서 직접 진입 지원

**`src/app/landing/page.tsx`**
- 코드 입력 바텀시트 하단에 "기존 회원이신가요?" 버튼 추가
- 클릭 시 `caddy_lookup_mode = phone` 저장 후 `/` 이동 → LicenseGuard가 전화번호 찾기 모드로 자동 진입

### 만료 고객 UX 흐름
```
4개월 사용 → 3개월 휴면 → 앱 재진입
    ↓ (localStorage 만료일 체크)
만료 모달 자동 출력
    ├── [온라인 갱신] → /subscribe?plan=6month
    ├── [딜러 통해 갱신] → 딜러 안내 텍스트
    └── [다른 코드 입력] → 잠금화면 복귀
    
또는 코드 분실 시:
앱 진입 → 잠금화면 → [기존 회원이신가요?]
    ↓
전화번호 입력 → 검색 → 만료 → 만료 모달
                        → 유효 → 코드 자동 입력 → 활성화
```

---

## ✅ 11회차 — 딜러 대시보드 이름 검색 + 고객 앱 만료일 실시간 갱신 + 설정 화면 이용권 정보

### 구현 내용

**`src/app/dealer/[token]/dashboard/page.tsx`**
- 기간 연장 탭 검색: 전화번호만 가능 → **이름 + 전화번호 복합 검색**으로 확장
- 이름 입력칸 추가 (전화번호 위)
- 이름만 입력 시 → Supabase ilike 검색 → 복수 결과 목록 카드 표시
- 동명이인 처리: 전화번호 끝 4자리 힌트(`010-****-5678`) + 이용권 코드 표시 → 클릭 선택
- 단일 결과 시 → 자동으로 선택
- 상태 추가: `renewName`, `renewResults[]`

**`src/components/layout/LicenseGuard.tsx`**
- `supabase` import 추가
- 앱 시작 시 (로컬 라이선스 유효 확인 후) 백그라운드로 Supabase DB에서 `expires_at` 조회 → `caddy_expires_at` 갱신
- 딜러가 기간 연장 시 고객이 앱 재시작하면 자동으로 새 만료일 반영

**`src/app/settings/page.tsx`**
- `Key`, `Copy`, `Check` 아이콘 import 추가
- `licenseCode`, `licenseExpiresAt`, `codeCopied` 상태 추가
- 설정 페이지 상단에 **이용권 정보 섹션** 추가:
  - 이용권 코드 (복사 버튼 포함)
  - 만료일
  - 상태: D-day 표시 (이용 중 D-x / 만료됨)

### 수정 파일
- `src/app/dealer/[token]/dashboard/page.tsx`
- `src/components/layout/LicenseGuard.tsx`
- `src/app/settings/page.tsx`

### 빌드 결과
- ✅ 20페이지 빌드 성공, 오류 0개
- ✅ http://localhost:4455 정상 실행 (PID 32612)
- ✅ TypeScript 타입 오류 없음

---

## ✅ 9회차 — 딜러 기간 연장 (갱신) 기능 구현 + A+B 하이브리드 확정

### 핵심 결정 사항
| 항목 | 결정 |
|------|------|
| 딜러 수익 모델 | **A+B 하이브리드** — 즉시 발급(현금) + 결제 링크(카드) 병행 |
| 갱신 방식 | **기간 연장** — 기존 코드 유지, `expires_at`만 갱신 |
| 코드 변경 금지 이유 | R2 백업 경로가 `{code}/latest.json`이므로 코드 변경 시 데이터 유실 |

### 구현 내용

**`src/lib/licenseUtils.ts`**
- `searchLicenseByPhone(phone)` — 전화번호로 기존 이용권 검색 (만료일/남은 일수 반환)
- `extendLicense({ licenseId, plan, days, dealerToken })` — 기존 코드 `expires_at` 연장 (코드 불변)

**`src/app/dealer/[token]/dashboard/page.tsx`**
- [신규 발급] / [기간 연장] 모드 토글 버튼 추가 (issue 탭 상단)
- 기간 연장 모드: 전화번호 입력 → 검색 → 기존 코드 조회 → 요금제 선택 → 연장
- 기간 연장 완료 화면: 기존 코드 그대로 표시 + 새 만료일 안내 + 복사 버튼
- 상태 변수 추가: `issueMode`, `renewPhone`, `renewResult`, `isExtending`, `extendedResult`

**`html/기획_딜러_수수료_토큰.html`**
- A+B 하이브리드 채택으로 결론 업데이트
- 수수료 배분 규칙 표: 갱신 케이스 전체 "신규 발급" → "기간 연장"으로 변경
- 헤더 메타: 미구현/미확정 → 채택 완료로 표시 변경
- 결정 버튼 영역: 구현 완료 상태로 업데이트

---

## ✅ 12회차 — 이용권 코드 대소문자 무시 비교 처리

### 배경
- 사용자 제보: `d1-NG9-WRQ` (소문자 d)로 입력 시 "이용권 확인 실패" 오류 발생
- 원인: `verifyLicenseAsync`에서 `.eq('code', trimmed)` 사용 → DB는 대문자 저장인데 소문자로 비교

### 수정 내용

**`src/lib/licenseUtils.ts`**
- `verifyLicenseAsync`: `.eq('code', trimmed)` → `.ilike('code', trimmed)` — 대소문자 무시 검색
- `searchLicenseByNamePhone`: SELECT 후 `data.code.toUpperCase() === trimmed.toUpperCase()` 비교 추가

**`src/components/layout/LicenseGuard.tsx`**
- 배경 만료일 갱신 쿼리: `.eq('code', stored)` → `.ilike('code', stored)` 로 변경

### 빌드 결과
- ✅ Compiled successfully (오류 0개)

---

## ✅ 13회차 — 프리미엄 티어 시스템 + 자동 복구 + 오늘 휴무 버그 수정

### 배경
- 오늘 휴무 버튼 클릭 시 반응 없음 (soft navigation 시 `date` state 미동기화)
- 백업 서비스가 이용권 찾기로 대체되어 수익 모델 필요
- 딜러 세일즈 포인트로 "클라우드 자동 복구" 연계 가능

### 가격 구조
| 구분 | 스탠다드 | 프리미엄 |
|------|---------|---------|
| 1개월 | 9,900원 | 12,900원 (+3,000) |
| 6개월 | 55,000원 | 69,000원 (+14,000) |
| 1년 | 99,000원 | 129,000원 (+30,000) |
| 자동 복구 | ❌ 수동 | ✅ 자동 |

### 구현 내용

**Supabase DB**
- `aone_pro_caddypro_licenses` 테이블에 `tier TEXT NOT NULL DEFAULT 'standard' CHECK (tier IN ('standard', 'premium'))` 컬럼 추가

**`src/lib/licenseUtils.ts`**
- `TIER_PRICES` 상수 추가 (스탠다드/프리미엄 요금표)
- `TierType = 'standard' | 'premium'` 타입 추가
- `issueVoucher`: `tier` 파라미터 추가 → DB INSERT에 tier 포함
- `verifyLicenseAsync`: SELECT에 tier 추가 → 반환값에 tier 포함
- `MemberSearchResult`: tier 필드 추가
- `searchLicenseByNamePhone`: SELECT에 tier 추가

**`src/components/layout/LicenseGuard.tsx`**
- localStorage `caddy_tier` 키 저장 (DB 조회 시 tier 동기화)
- 프리미엄 + localStorage 비어있으면 R2 `/api/backup/download` 자동 복구
- 복구 완료 토스트 모달 UI (3.5초 자동 닫힘)
- `pending_code` 처리 시 tier도 localStorage에 저장

**`src/lib/paymentService.ts`**
- `PORTONE_PRODUCTS_PREMIUM` 상수 추가 (프리미엄 상품명/금액)
- `requestCaddyPayment`: `tier` 파라미터 추가 → tier별 동적 상품 선택
- sessionStorage에 tier도 저장

**`src/app/subscribe/page.tsx`**
- `Shield`, `Star` 아이콘 추가
- `selectedTier` state 추가 + URL `?tier=premium` 파라미터 읽기
- 스탠다드/프리미엄 선택 카드 UI 추가
- 요금제 선택을 tier별 상품으로 동적 전환
- 결제 버튼 금액 tier 기반으로 표시
- payment API 호출 및 sessionStorage 복구 시 tier 전달

**`src/app/dealer/[token]/dashboard/page.tsx`**
- `issueTier` state 추가 (`standard` | `premium`)
- `issueVoucher` 호출에 `tier: issueTier` 추가
- 발급 폼에 스탠다드/프리미엄 선택 카드 UI 추가

**`src/app/api/payment/complete/route.ts`**
- `VALID_PLANS`에 `premiumAmount` 필드 추가 (프리미엄 가격)
- body에서 `tier` 수신 → `resolvedTier`로 정규화
- 금액 검증: tier에 따라 `amount` vs `premiumAmount` 분기
- `issueVoucher` 호출에 `tier: resolvedTier` 추가

**`src/app/schedule/page.tsx`**
- `useEffect([dateParam])` 동기화 추가 → 오늘 휴무 버튼 soft navigation 수정

### localStorage 키 현황
| 키 | 용도 |
|----|------|
| `caddy_license_key` | 이용권 코드 |
| `caddy_expires_at` | 만료일 |
| `caddy_tier` | 이용권 등급 (신규) |
| `app-store-schedules` | 스케줄 데이터 |

### 빌드 결과
- ✅ TypeScript 오류 0개 (정적 분석)

---

## ✅ 12회차 — 랜딩 진입 플로우 전면 개편 + 결제 검증 강화 + 딜러 귀속 갱신

> 커밋: `f13d6c1` | 날짜: 2026-04-04

### 핵심 변경 사항

#### 1. 랜딩 바텀시트 history 기반 전면 재설계

**문제**: `setSheet('start')` 직접 호출 시 `history.pushState`와 불일치 → 하드웨어 뒤로가기가 실제 페이지 이동으로 처리됨

**해결**:
- `openSheet(s)` 헬퍼 추가 → `pushState({ sheetState: s })` + `setSheet(s)` 동시 처리
- `closeSheet()` / `goBack()` → `window.history.back()` (history와 React state 자동 동기화)
- popstate 핸들러: `capture: true` (Next.js Router보다 먼저 실행)
- 마운트 시 `replaceState({ sheetState: 'none' })` 기준점 설정
- 모든 `setSheet('...')` 버튼 → `openSheet('...')` 교체

**결과**: sheet='start' 상태에서 뒤로가기 → sheet='none' (랜딩 기본화면) 정상 동작

#### 2. 기존 회원 찾기 UI 개선

| 항목 | 변경 |
|------|------|
| 이용권 코드 입력란 | **제거** (코드 모르니까 회원 찾는 것) |
| 검색 버튼 | disabled 조건 제거 → **항상 활성** |
| 검색 클릭 시 검증 | **에러 모달**로 안내 |

**에러 모달 케이스:**

| 상황 | 메시지 |
|------|--------|
| 아무것도 입력 안 함 | 이름 또는 전화번호를 입력해주세요. |
| 이름만 입력 | 전화번호도 함께 입력해주세요.\n이름만으로는 본인 확인이 어렵습니다. |
| 전화번호 9자리 미만 | 전화번호를 정확히 입력해주세요.\n(예: 010-1234-5678) |

#### 3. 전화번호 검색 DB 매칭 오류 수정

**문제**: DB에 `010-8468-9186` (대시 포함)이 저장되어 있는데, 검색은 `01084689186`(숫자만)으로 비교 → 매칭 실패

**해결**: OR 조건에 대시 포함/제외 양방향 추가
```typescript
query.or(`user_phone.eq.${digits}, user_phone.ilike.%${digits}%, user_phone.eq.${formatted}, user_phone.ilike.%${formatted}%`)
```

#### 4. 만료 이용권 갱신 시 딜러 자동 귀속

- `MemberSearchResult`에 `issuedBy` 필드 추가
- DB SELECT에 `issued_by` 컬럼 포함
- [온라인으로 갱신하기] 링크: `issued_by` 있으면 `?ref={딜러토큰}` 자동 포함

```
딜러 고객 갱신: /subscribe?plan=6month&ref=딜러토큰 → 딜러 수수료 자동 귀속
일반 고객 갱신: /subscribe?plan=6month               → 본사 직접 매출
```

#### 5. 결제 페이지 (`subscribe/page.tsx`) 검증 강화

| 항목 | 변경 |
|------|------|
| 전화번호 검증 | `< 10` → `< 11` (11자리 미만 차단) |
| 입력 미충족 | 인라인 에러 → **팝업 모달** 교체 |
| 결제 실패/취소 | `setError` → `setModalMsg` (모달 표시) |
| 모바일 뒤로가기 | `visibilitychange` 이벤트 → loading 자동 해제 |

#### 6. 테스트 데이터 삽입 (Supabase)

```sql
-- 만료된 테스트 이용권
이름: 홍길동 / 전화: 010-1234-5678
코드: SM-TEST-EXP / 만료: 2025-12-31 / is_active: false
```

### 수정 파일
- `src/app/landing/page.tsx` — 전면 개편
- `src/lib/licenseUtils.ts` — 검색 로직 + MemberSearchResult 타입 개선
- `src/app/subscribe/page.tsx` — 검증 강화 + 모달 UX

### 빌드 결과
- ✅ 20페이지 빌드 성공, 오류 0개
- ✅ http://0.0.0.0:4455 정상 실행
- 커밋: `f13d6c1`

---

## 📋 12회차 미완료 / 다음 작업 후보

- [ ] Vercel 환경변수 추가 후 재배포
- [ ] 딜러 정산 요청 알림 (카카오 알림톡 or 이메일)
- [ ] 이용권 만료 전 자동 알림
- [ ] subscribe 페이지: `payMethod` URL 파라미터 분기 (가상계좌/실시간이체)

---

## ✅ 13회차 — golf-caddy-pro 롤백 + 배포 분리 + 계정별 스토리지 완전 분리

> 날짜: 2026-04-04

### 핵심 작업

#### 1. golf-caddy-pro 테스터 롤백
- **문제**: 테스터 50명+ 사용 중인 golf-caddy-pro.vercel.app이 Supabase 연동 신버전 코드로 접속 불가
- **해결**: `git push origin aeae778:master --force` → 구버전으로 강제 롤백
- **결과**: golf-caddy-pro.vercel.app 정상 복구

#### 2. 배포 분리 고정
- caddy (new_origin = `caddy_pro_enterprise.git`) → `caddy-pink.vercel.app` 만 사용
- golf-caddy-pro.git (origin) = 구버전 동결, 절대 건드리지 않음
- `.github/copilot-instructions.md`에 배포 규칙 추가

#### 3. 이용권 발급 즉시 expires_at 설정
> 커밋: `4efc65d`
- **문제**: 발급 시 `expires_at = NULL` → 이용내역 탭 표시 안 됨
- **해결**: `issueVoucher()` 내에서 오늘 기준 + days 계산 후 즉시 저장
- DB NULL 값 SQL 일괄 업데이트 실행
- `created_at` 컬럼 추가 (ALTER TABLE)

#### 4. 계정별 localStorage 완전 분리
> 커밋: `be955c2` → `305c180` → `caed3c4` → `9556825`

**구조**:
```
caddy-manager-storage           → 현재 로그인 계정의 활성 데이터
caddy-manager-storage_{code}    → 코드별 백업 (로그아웃/전환 시 저장)
caddy_active_key                → 현재 로그인된 코드
caddy_user_name_{code}          → 계정별 이름 캐시 (분리)
```

- **LicenseGuard.tsx**: 계정 전환 감지 → 기존 데이터 백업 → 새 계정 데이터 복원 → reload
- **store.ts**: setItem 시 `caddy-manager-storage_{activeKey}`에도 동기 저장
- **로그아웃**: 현재 데이터 백업 → 메인 스토리지 삭제 → 키 전체 삭제 → `/landing`
- **로그인**: 무조건 메인 스토리지 삭제 → 해당 코드 백업 복원 → reload
- **F5 새로고침 버그**: `activeKey 있을 때만` 계정 전환 처리 (없으면 건드리지 않음)

#### 5. 홈 화면 UX 개선
> 커밋: `5080214` → `1cda541` → `2751770` → `f7cf5b6`

- **이름 환영 메시지**: `caddy_user_name_{code}` 캐시 → Supabase fallback
- **마스터키 0827**: 홈에서 "관리자" 표시
- **시작하기 버튼**: `caddy_license_key` 캐시 있으면 바로 `/home` 이동
- **로그아웃 버튼**: 홈 화면 상단에 추가

#### 6. 랜딩 회원검색 전화번호 오류 수정
> 커밋: `efc6823`

- **문제**: DB 저장 형식 `010-1111-1111` vs 입력 `01011111111` 불일치
- **해결**: 숫자 추출 후 하이픈 포함/미포함 양방향 OR 검색
- `catch (err)` → `console.error('[회원검색 오류]', err)` 에러 로깅 추가

#### 7. LicenseGuard.tsx 빌드 오류 수정
> 커밋: `8039975`

- **원인**: `if (!activeKey)` 블록 종료 뒤 `}` 중복 → TypeScript Expression expected 에러
- **해결**: 중복 `}` 제거

### 커밋 히스토리 (이번 세션)
| 커밋 | 내용 |
|------|------|
| `4efc65d` | fix: 이용권 발급 즉시 expires_at 설정 |
| `efc6823` | fix: 랜딩 전화번호 정규화 + 에러 로깅 |
| `be955c2` | feat: 계정별 localStorage 분리 |
| `305c180` | fix: F5 새로고침 시 activeKey 없으면 계정전환 건너뜀 |
| `5080214` | feat: 홈 이름 환영 메시지 계정별 캐시 |
| `1cda541` | feat: 마스터키 0827 관리자 표시 |
| `2751770` | feat: 시작하기 캐시 체크 후 /home 이동 |
| `f7cf5b6` | feat: 홈 로그아웃 버튼 추가 |
| `caed3c4` | fix: 로그아웃 시 스토리지 초기화 |
| `9556825` | fix: 신규 로그인 시 스토리지 완전 초기화 + 이름 캐시 계정별 분리 |
| `8039975` | fix: LicenseGuard 괄호 오류 수정 |

### 수정 파일
- `src/components/layout/LicenseGuard.tsx`
- `src/lib/store.ts`
- `src/lib/licenseUtils.ts`
- `src/app/home/page.tsx`
- `src/app/landing/page.tsx`
- `.github/copilot-instructions.md`

### 환경 현황
| 항목 | 값 |
|------|------|
| 배포 URL | https://caddy-pink.vercel.app |
| 배포 레포 | caddy_pro_enterprise.git (new_origin) |
| 구버전 동결 | golf-caddy-pro.git (aeae778) |
| R2 환경변수 | caddy Vercel 프로젝트에 등록 확인 |
| Vercel Project ID | prj_bGF0Qi7a1cF9EGthyYOjXdjQLDYO |

---

## 📋 현재 미완료 / 다음 작업 후보

### 기능 개발
- [ ] 지출내역 탭 (캐디용품/영수증/생활비/기타) — Supabase `aone_pro_caddypro_expenses` 테이블
- [ ] 영수증 사진 업로드 (브라우저 Canvas 압축 → R2 저장)
- [ ] OCR 자동 금액 추출 (Gemini Vision API)
- [ ] 구글 드라이브 연결 옵션 (이용자 본인 클라우드)

### DB 정규화 전환 준비 (테스터 전환 대비)
- [ ] Supabase 정규화 테이블 설계/생성 SQL 작성
  - `aone_pro_caddypro_schedules`
  - `aone_pro_caddypro_clients`
  - `aone_pro_caddypro_transactions`
  - `aone_pro_caddypro_fee_settings`
  - `aone_pro_caddypro_expenses`
- [ ] 테스터 백업 JSON → Supabase 마이그레이션 스크립트
- [ ] 구버전 "내보내기" → 신버전 "가져오기" 도구

### 기존 미완료
- [ ] 딜러 정산 요청 알림 (카카오 알림톡 or 이메일)
- [ ] 이용권 만료 전 자동 알림
- [ ] subscribe 페이지: `payMethod` URL 파라미터 분기

---

## ✅ 14회차 — 안 B Phase 1~5: Supabase 동기화 + 수입 입력 + 영수증 OCR + 세금 개선

> 날짜: 2026-04-04 ~ 04-05  
> 커밋: `011ff82` → `fcbb9aa` → `3586d8c` → `49d8e09` → `c54656f`

### 핵심 구현 내용

#### Phase 1 — Supabase 동기화 연결
- `supabase_tables.sql` — 5개 정규화 테이블 DDL 작성 (사용자가 SQL Editor에서 직접 실행)
  - `aone_pro_caddypro_schedules`, `transactions`, `clients`, `fee_settings`, `expenses`
- `src/lib/supabaseDB.ts` **신규** — localStorage ↔ Supabase 하이브리드 동기화
  - silent 패턴 (fire-and-forget): 실패해도 앱 정상 동작
  - `syncAddSchedule`, `syncUpdateSchedule`, `syncDeleteSchedule`
  - `syncAddTransaction`, `syncDeleteTransaction`
  - `syncAddClient`, `syncUpdateClient`, `syncDeleteClient`
  - `syncFeeSettings`, `migrateLocalDataToSupabase`
- `src/lib/store.ts` — `addTransaction/deleteTransaction`, `addSchedule/updateSchedule/deleteSchedule` Supabase 동기화 추가
- `src/lib/store.ts` — `ExpenseCategory`에 `'caddy_fee' | 'tip' | 'over_fee'` 추가

#### Phase 2 — 홈 퀵 수입 입력 모달
- `src/app/home/page.tsx` — 오늘 수입 빠른 입력 모달 (중앙 팝업)
  - 입력: 캐디피 / 팁·오버피 / 기타수입 / 메모
  - 기본값: `fmtInput(String(feeSettings.shift1))` — 콤마 포맷 적용
  - 저장 시 `addTransaction()` 호출 → Supabase 동기화
- 연간 수입 카드 추가 (총 라운드 / 18홀 / 9홀 카운트)

#### Phase 3 — Supabase 마이그레이션 UI
- `src/app/settings/page.tsx` — "지금 바로 동기화하기" 버튼 추가
  - `migrateLocalDataToSupabase()` 호출, 상태 표시 (migrating / done / error)

#### Phase 4 — 영수증 카메라 + Canvas 압축 + Gemini Vision OCR
- `src/app/money/page.tsx` — expense 모달 내 카메라 버튼 추가
  - Canvas 압축: 1200px, JPEG 0.75 (10MB → ~400KB)
  - 업로드: `/api/receipt/upload` POST (FormData)
  - OCR 자동 입력: `data.ocrAmount` → `amount`, `data.ocrMemo` → `memo`
- `src/lib/r2Client.ts` — `uploadReceipt()` 함수 추가
  - 저장 경로: `caddy-manager-pro/receipts/{license_code}/{timestamp}.jpg`
- `src/app/api/receipt/upload/route.ts` **신규**
  - Gemini Vision OCR (`GEMINI_API_KEY` 있을 때만 실행)
  - 반환: `{ success, url, ocrAmount, ocrMemo }`

#### Phase 5 — 세금 화면 개선
- `src/app/tax/page.tsx` — transactions 기반 수입/경비 자동 합산
  - `yearlyTxIncome` + `yearlyScheduleIncome` → `Math.max`로 큰 값 사용
  - `yearlyExpense` — transactions expense 합계
  - 수입 요약 카드 + "N원으로 로드" 버튼

### UI 수정 (모달 3건)
| 수정 내용 | 커밋 |
|-----------|------|
| 수입 입력 모달: 바텀시트 → 중앙 팝업 | `3586d8c` |
| 콤마 포맷 + 바텀 네비 겹침 (`max-h-[calc(100dvh-80px)]`) | `49d8e09` |
| 팁 라벨: "오버피" → "팁·오버피" / "기타수입" | `c54656f` |

### 수정 파일
- `src/lib/supabaseDB.ts` **신규**
- `src/lib/store.ts`
- `src/app/home/page.tsx`
- `src/app/settings/page.tsx`
- `src/app/money/page.tsx`
- `src/lib/r2Client.ts`
- `src/app/api/receipt/upload/route.ts` **신규**
- `src/app/tax/page.tsx`
- `supabase_tables.sql` **신규** (루트)

### 빌드 결과
- ✅ TypeScript 오류 0개

---

## ✅ 15회차 — OCR 이용권 차별화 + 재구매 UX 보완 + 영수증 품질 개선

> 날짜: 2026-04-05

### 기획 확정 사항

#### R2 요금 구조 확인
| 항목 | 무료 | 유료 단가 |
|------|------|----------|
| 저장 | 10GB/월 | $0.015/GB-월 |
| Class A (업로드) | 100만 건/월 | $4.50/100만 건 |
| Class B (다운로드) | 1,000만 건/월 | $0.36/100만 건 |
| 송신료 | **완전 무료** | **완전 무료** |
> 1만 명 기준 영수증 저장 비용 월 $2 미만 → 셀링포인트로 활용 가능

#### OCR plan별 차별화 확정 (안 A)
| 이용권 | OCR 월 횟수 | 영수증 보관 |
|--------|------------|------------|
| 1개월권 | **5회** | 30일 |
| 6개월권 | **30회** | 6개월 |
| 1년권 | **무제한** | 1년 |

#### 재구매 시 이용권 연결 전략 확정 (안 B)
```
기존 만료 코드 있음 → extendLicense() → 코드 불변, 기간만 연장
→ R2 경로 그대로 유지 → 데이터 완전 연속성 보장

searchLicenseByNamePhone 결과가 여러 개:
→ 유효한 코드 우선 표시
→ 전부 만료 시 → 가장 최근 만료(= 데이터 가장 많은) 코드 선택
```

### 구현 내용

#### `src/lib/licenseUtils.ts`
- `verifyLicenseAsync` 반환값에 `plan?: PlanType` 추가
- 마스터키 `0827` → `plan: 'year'` 반환
- `searchLicenseByNamePhone` 결과 정렬 로직 추가:
  ```typescript
  // 유효 코드 우선, 만료 중에서는 최근 만료 순
  results.sort((a, b) => {
    if (!a.isExpired && b.isExpired) return -1;
    if (a.isExpired && !b.isExpired) return 1;
    return new Date(b.expiresAt).getTime() - new Date(a.expiresAt).getTime();
  });
  ```

#### `src/components/layout/LicenseGuard.tsx`
- 앱 시작 시 배경 DB 조회 SELECT에 `plan` 컬럼 추가
- `caddy_plan` localStorage 저장 (tier와 동일한 패턴)
- `pending_code` 처리 시 `caddy_plan` 저장

#### `src/app/money/page.tsx`
- OCR 월별 제한 로직 추가:
  - `getOcrLimit()`: `caddy_plan` 기반 → `year=999 / 6month=30 / month=5`
  - `getOcrCount()`: `caddy_ocr_YYYY-MM` localStorage 키로 월별 카운트
  - `incrementOcrCount()`: 업로드 `success:true` 시에만 카운트 (확정 업로드 시점)
- UI: 잔여 횟수 배지 (`이번 달 N/M회 남음`) + 한도 초과 시 버튼 비활성화
- 오류 모달: 한도 초과 알림 + 업그레이드 안내
- Canvas 최소폭 보장:
  ```javascript
  // 변경 전: 세로 긴 영수증이 240px로 찌그러짐
  // 변경 후: 최소 400px 너비 보장 → 세로만 길어지고 가로 유지
  const MIN_W = 400;
  canvas.width = Math.max(img.width * ratio, MIN_W);
  canvas.height = img.height * (canvas.width / img.width);
  ```
- 버튼 라벨: "영수증 합계 부분 찍기 / OCR"
- 힌트 문구: `💡 긴 영수증은 합계금액이 있는 아랫부분만 찍으면 정확도가 높아요`

### localStorage 키 현황 (최신)
| 키 | 용도 |
|----|------|
| `caddy_license_key` | 이용권 코드 |
| `caddy_expires_at` | 만료일 |
| `caddy_tier` | 이용권 등급 (standard/premium) |
| `caddy_plan` | 요금제 (month/6month/year) **신규** |
| `caddy_ocr_YYYY-MM` | 월별 OCR 사용 횟수 **신규** |
| `caddy-manager-storage` | 현재 계정 활성 데이터 |

### 수정 파일
- `src/lib/licenseUtils.ts`
- `src/components/layout/LicenseGuard.tsx`
- `src/app/money/page.tsx`

---

## ✅ 56회차 — SMS 연동: 알리고 → 솔라피로 교체 + 모달 알림 추가

> 날짜: 2026년 4월 8일  
> 커밋: `6347e02` → `71dac03` → `7395bad` → `5e3ea45` → `b3ae390`

### 배경

**문제**: 알리고 API는 **고정 IP 등록 필수** 정책
- Vercel 서버리스: 유동 IP (매번 다름)
- 결과: API 호출 시 "인증오류-IP" 에러 → 문자 미발송

**시도 & 실패**:
1. Supabase pg_net RPC: JSON만 지원 (form-urlencoded 불가)
2. Supabase Edge Function: 고정 IP 아님 (43.200.163.218 → 43.200.4.241로 변경됨)
3. 닷홈 무료호스팅: PHP 실행 불가
4. 카페24: 무료호스팅 서비스 폐지
5. 닷홈 웹호스팅: PHP 설정 불가

**최종 해결책**: **솔라피(Solapi)** 교체 (IP 제한 없음)

### 구현 내용

#### 1단계: solapi npm 패키지 설치
```bash
npm install solapi
```

#### 2단계: `src/lib/aligo.ts` → 솔라피로 교체

```typescript
/**
 * SMS 발송 유틸 (솔라피 기반)
 * https://solapi.com
 */
import { SolapiMessageService } from 'solapi';

export async function sendSMS(params: AligoSendParams): Promise<{ ok: boolean; message?: string }> {
  const apiKey = process.env.SOLAPI_API_KEY;
  const apiSecret = process.env.SOLAPI_API_SECRET;
  const sender = process.env.SOLAPI_SENDER ?? process.env.ALIGO_SENDER;

  if (!apiKey || !apiSecret || !sender) {
    console.error('[solapi] 환경변수 미설정');
    return { ok: false, message: '솔라피 환경변수 미설정' };
  }

  const receiver = params.receiver.replace(/\D/g, '');
  const msgType = params.msg_type ?? (Buffer.byteLength(params.msg, 'utf8') > 90 ? 'LMS' : 'SMS');

  try {
    const service = new SolapiMessageService(apiKey, apiSecret);
    await service.sendOne({
      to: receiver,
      from: sender,
      text: params.msg,
      type: msgType,
      ...(msgType === 'LMS' && params.title ? { subject: params.title } : {}),
    });
    return { ok: true };
  } catch (e: unknown) {
    const err = e as { message?: string };
    console.error('[solapi] 발송 실패:', err);
    return { ok: false, message: err?.message ?? '발송 실패' };
  }
}
```

**주의**: ~~`import SolapiMessageService from 'solapi'`~~ → **named import 사용**
- **정정**: `import { SolapiMessageService } from 'solapi'` (default export 아님)

#### 3단계: Vercel 환경변수 등록

```
SOLAPI_API_KEY=NCSIYJUZJOSIQQ94
SOLAPI_API_SECRET=PQYWGKXJFLJSDPIXSSCK8FTK12H8WTO0
SOLAPI_SENDER=01027377229
```

**삭제된 환경변수**:
- ALIGO_API_KEY
- ALIGO_USER_ID
- ALIGO_SENDER

#### 4단계: fetch URL trailing slash 추가

`next.config.mjs`에 `trailingSlash: true` 설정 때문에 POST 요청 시 308 리다이렉트 → body 소실

**수정**: `licenseUtils.ts`에서 API 호출 경로에 슬래시 추가
```typescript
// 변경 전: '/api/notify/welcome' → 308 리다이렉트 (body 소실)
// 변경 후: '/api/notify/welcome/' → 정상 작동
const res = await fetch('/api/notify/welcome/', { ... });
const res = await fetch('/api/notify/extend/', { ... });
```

#### 5단계: SMS 결과 모달 추가

**상태 관리**:
```typescript
const [smsModal, setSmsModal] = useState<{ ok: boolean; msg: string } | null>(null);
```

**SMS 발송 완료 시**:
```typescript
if (result.smsOk === false) {
    setSmsModal({ ok: false, msg: result.smsMessage ?? '알 수 없음' });
} else if (userPhone) {
    setSmsModal({ ok: true, msg: '가입 문자가 고객에게 발송되었습니다.' });
}
```

**UI 렌더링**:
```tsx
{smsModal && (
    <div className="fixed inset-0 z-[200] flex items-center justify-center bg-black/60 p-6" onClick={() => setSmsModal(null)}>
        <div className="bg-stone-900 border border-stone-700 rounded-3xl p-7 w-full max-w-xs text-center space-y-4 shadow-2xl" onClick={e => e.stopPropagation()}>
            <div className={`w-14 h-14 rounded-full flex items-center justify-center mx-auto text-3xl ${smsModal.ok ? 'bg-green-900/40' : 'bg-red-900/40'}`}>
                {smsModal.ok ? '✅' : '⚠️'}
            </div>
            <div>
                <p className="text-white font-bold text-base mb-1">{smsModal.ok ? '문자 발송 성공' : '문자 발송 실패'}</p>
                <p className="text-stone-400 text-sm">{smsModal.msg}</p>
            </div>
            <button onClick={() => setSmsModal(null)} className="w-full bg-amber-500 hover:bg-amber-400 text-black font-bold py-3 rounded-2xl text-sm">확인</button>
        </div>
    </div>
)}
```

### 적용 화면
| 페이지 | 적용 상태 |
|--------|----------|
| 관리자 코드발급 탭 | ✅ 모달 알림 |
| 딜러 대시보드 코드발급 || 신규/신용 | ✅ 모달 알림 |
| 딜러 발급 페이지 | ✅ 모달 알림 |

### 테스트 결과

**수신 내역**:
| 수신번호 | 시간 | 타입 | 상태 |
|---------|------|------|------|
| 010-3083-2585 | 14:05 | LMS | ✅ 발송(이통사 수신) |
| 010-2737-7229 (본인) | 14:07 x2 | SMS | ✅ 수신 확인 |
| 010-8468-9186 | 14:22 | LMS | ✅ 발송(이통사 수신) |

**원인 분석**:
- `RESULT: 2` = 이통사 수신 완료
- 일부 못 받은 사용자: **폰 문자 앱 "광고/공지" 탭** 자동 분류
  - `[Web발신]` 태그 때문에 스팸이 아닌 "공지" 탭으로 자동분류
  - 기본 태그 탭이 아님

### 수정 파일
- `src/lib/aligo.ts` — `SolapiMessageService` import + 함수 전체 교체
- `src/lib/licenseUtils.ts` — `/api/notify/welcome/` 과 `/api/notify/extend/` 에 trailing slash 추가
- `src/app/admin/page.tsx` — `smsModal` 상태 + UI 추가
- `src/app/dealer/[token]/dashboard/page.tsx` — `smsModal` 상태 + UI 추가
- `src/app/dealer/[token]/page.tsx` — `smsModal` 상태 + UI 추가
- `package.json` — `solapi` 패키지 추가

### 커밋 히스토리
| 커밋 | 내용 |
|------|------|
| `6347e02` | fix: 알리고 → 솔라피 SMS 교체 (IP 제한 없음) |
| `71dac03` | fix: 솔라피 named import로 수정 (i.default is not a constructor) |
| `7395bad` | fix: fetch URL trailing slash 추가 (308 리다이렉트 방지) |
| `5e3ea45` | feat: 코드 발급 후 SMS 성공/실패 토스트 메시지 추가 |
| `b3ae390` | feat: SMS 결과 토스트 → 모달 팝업으로 교체 |

### 빌드 결과
- ✅ 20페이지 빌드 성공, 오류 0개
- ✅ Vercel 재배포 성공 (`caddy-pink.vercel.app`)

### 솔라피 자격증명
| 항목 | 값 |
|------|------|
| API Key | `NCSIYJUZJOSIQQ94` |
| API Secret | `PQYWGKXJFLJSDPIXSSCK8FTK12H8WTO0` |
| 발신번호 | `01027377229` |
| 출처.txt | `b-messenger` 프로젝트 폴더 내 |

### 다음 작업 (카카오톡 템플릿)
- [ ] 솔라피 콘솔에서 **5가지 템플릿** 등록 및 승인
  1. CaddyManagerPro_Welcome — 가입 환영
  2. CaddyManagerPro_Extend — 이용권 연장
  3. CaddyManagerPro_ExpireSoon — 만료 예정 (7일전)
  4. CaddyManagerPro_Schedule — 라운드 예약 알림
  5. CaddyManagerPro_PersonalEvent — 개인별 프로모션
- [ ] 상세 가이드: `SOLAPI_KAKAOTALK_TEMPLATE_GUIDE.md` 작성 완료 ✅

## ✅ 19회차 — OCR 완전 활성화 (Supabase 횟수 관리 + Gemini API Key 등록)

### 배경
- 기존 OCR 횟수: localStorage 기반 → 기기 변경 시 초기화 문제
- Gemini API Key 미등록 → OCR 실제 동작 불가

### 구현 내용

#### Supabase 테이블 생성
```sql
CREATE TABLE aone_pro_caddypro_ocr_usage (
  license_key TEXT PRIMARY KEY,
  month TEXT NOT NULL,
  count INT DEFAULT 0,
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```
- 대문자 변환 트리거 적용

#### `src/app/api/db/ocr-usage/route.ts` (신규)
- GET: 현재 월 사용 횟수 조회
- POST: 사용 횟수 +1 (upsert)

#### 환경변수
- `.env.local`: `GEMINI_API_KEY=AIzaSyA7eVBuEB74Vi7Xm7m9QXKk5hyUv_-r9J0` 추가
- Vercel 환경변수: `vercel env add GEMINI_API_KEY` 등록 완료

#### `src/app/money/page.tsx`
- OCR 횟수: localStorage(`caddy_ocr_YYYY-MM`) → Supabase(`ocr_usage`) 기반으로 교체
- `getOcrCount()`: Supabase GET 조회
- `incrementOcrCount()`: Supabase POST +1

### 커밋
- `4b2f487` — "feat: OCR 완전 활성화 (Supabase 횟수 관리 + Gemini API Key 등록)"

---

## ✅ 20회차 — 로그아웃 store 초기화 + 일정폼 날짜 표시 + 만료 배너 개선 + OCR 결과 표시

### 로그아웃 store 초기화 (`home/page.tsx`)
- 로그아웃 시 Zustand store 완전 초기화:
  ```typescript
  useAppStore.setState({ schedules: [], clients: [], transactions: [], ... _initialized: false });
  ```
- `caddy_plan` localStorage도 삭제 대상에 추가
- 커밋: `db206ad`

### 일정폼 날짜 표시 (`schedule/page.tsx`)
- form 뷰 헤더: "← 목록으로" 버튼 옆에 날짜(MM월 DD일) 추가
- 커밋: `48f5593`

### 만료 배너 개선 (`LicenseGuard.tsx`)
- [갱신하기] 버튼 → `/subscribe?plan=6month`
- X 닫기 시 `caddy_expire_banner_dismissed` = 오늘 날짜 → 하루 1회 억제
- 커밋: `74f3629`

### OCR 결과 표시 개선 (`money/page.tsx`)
- `ocrResult` state 추가
- OCR 성공: "✅ OCR 인식 완료 — N원 자동 입력됨"
- OCR 실패: "⚠️ 금액 인식 실패 — 직접 입력해주세요"
- 커밋: `6d9d7ce`

---

## ✅ 21회차 — 세무 기준 경비 카테고리 재편 + OCR 금액 자동입력 버그 수정

### 배경
- 영수증 OCR 후 "이 입력란을 작성하세요" 오류 발생 → HTML `required` + OCR 미반영 버그
- `food / other` 등 기존 카테고리가 세무 기준에 맞지 않는 문제

### 카테고리 재편 (안 A)

| 새 카테고리 | 이전 | 세무 처리 |
|------------|------|---------|
| `transport` | transport | ✅ 경비 인정 |
| `meal` | food | ✅ 경비 인정 |
| `gear` | gear | ✅ 경비 인정 |
| `etc_expense` | other | ✅ 경비 인정 |
| `personal` | (신규) | ❌ 경비 불인정 |

### 구현 내용

#### `src/lib/store.ts`
```typescript
// 변경 전
export type ExpenseCategory = 'food' | 'transport' | 'gear' | 'other' | ...
// 변경 후
export type ExpenseCategory = 'transport' | 'gear' | 'meal' | 'etc_expense' | 'personal' | ...
```

#### `src/app/api/receipt/upload/route.ts`
- Gemini 프롬프트: amount + merchant + category(transport/meal/gear/etc_expense/personal) 동시 추출
- `ocrCategory` 파싱 + 응답에 포함

#### `src/app/money/page.tsx`
- 카테고리 기본값: `'food'` → `'meal'`
- OCR 응답 `ocrCategory` → `setCategory()` 자동입력
- `required` 제거 → JS 검증으로 교체 (`numAmount <= 0` → alert)
- 카테고리 UI: 5개 버튼 (교통비✅ / 업무식비✅ / 업무용품✅ / 기타경비✅ / 개인지출⚠️)

#### `src/app/tax/page.tsx`
- `DEDUCTIBLE_CATS`: `['transport', 'meal', 'gear', 'etc_expense']`
- `yearlyDeductibleExpense`: 경비 인정 합계
- `yearlyPersonalExpense`: 개인지출(personal) 합계
- 세금 탭: "✅ 경비 인정 지출" + "⚠️ 개인지출 (경비 불인정)" 분리 표시

### 수정 파일
- `src/lib/store.ts`
- `src/app/api/receipt/upload/route.ts`
- `src/app/money/page.tsx`
- `src/app/tax/page.tsx`

### 커밋
- `6194d92` — "feat: 세무 기준 경비 카테고리 재편 + OCR 자동분류 + 금액 required 버그 수정"

---

## 📋 다음 작업 후보

- [ ] caddy-pink.vercel.app 배포 완료 확인
- [ ] OCR 실제 동작 확인 (영수증 사진 → 금액 자동입력 + 카테고리 자동분류)
- [ ] 다른 사용자 마이그레이션 (김경준 DL-NG9-WRQ 등)
- [ ] 캐디피 / 팁 / 초과 카테고리 세부 처리 검토

### 빌드 결과
- ✅ TypeScript 오류 0개 (`npx tsc --noEmit`)

---

## ✅ 22회차 — OCR 버그 완전 수정 + 영수증 R2-DB 연결 + 내역 썸네일 표시

> 날짜: 2026-04-05  
> 커밋: `fc02525` (OCR 모델), `53db35b` (영수증 DB 연결)

### 배경
- OCR 영수증 찍어도 "금액을 입력하세요" 오류 반복 → Gemini 모델 문제
- R2에 영수증 저장되지만 DB에 연결되지 않아 프로그램에서 볼 수 없는 문제

### 1. Gemini 모델 교체 (커밋 `fc02525`)

| 모델 | 결과 | 원인 |
|------|------|------|
| `gemini-1.5-flash` | ❌ 404 | v1beta에서 제거됨 |
| `gemini-2.0-flash` | ❌ 429 | 쿼터 초과 |
| `gemini-2.0-flash-lite` | ❌ 429 | 쿼터 초과 |
| `gemini-2.5-flash` | ✅ 성공 | 현재 사용 모델 |

**`src/app/api/receipt/upload/route.ts`**
- 모델명: `gemini-1.5-flash` → `gemini-2.5-flash`
- `res.ok` 아닐 때 에러 로깅 추가
- 문자열 amount도 parseInt로 파싱 처리

### 2. /test-ocr 디버그 페이지 추가 (커밋 `fc02525`)
- `src/app/test-ocr/page.tsx` **신규**
- OCR API RAW 응답 + 파싱 결과 화면 표시 (관리자 디버그 도구)
- 미들웨어 우회 없이 자유 접근

### 3. 영수증 R2-DB 연결 (커밋 `53db35b`)

#### Supabase 컬럼 추가
```sql
ALTER TABLE aone_pro_caddypro_transactions
  ADD COLUMN IF NOT EXISTS receipt_url TEXT;
```

#### `src/lib/store.ts`
```typescript
export interface Transaction {
    // ...
    receiptUrl?: string;   // 신규 추가
}
```

#### `src/app/api/db/transactions/route.ts`
- GET: `receiptUrl: r.receipt_url ?? undefined` 추가
- POST upsert: `receipt_url: t.receiptUrl ?? null` 추가

#### `src/app/money/page.tsx`
- handleSubmit: `receiptUrl: receiptUrl ?? undefined` 포함
- 내역 카드 구조 변경 (`overflow-hidden` 컨테이너)
- `item.receiptUrl` 있을 때 카드 하단에 영수증 썸네일 표시
- 썸네일 탭 시 원본 R2 URL 새 탭으로 열림

### R2 파일 현황
| 경로 | 파일 수 | 비고 |
|------|---------|------|
| `receipts/DC-AWA-S72/` | 3개 | 10개 실수 삭제됨 (복원 불가) |
| `receipts/TEST-OCR-000/` | 0개 | 테스트 파일 정리 완료 |

### 수정 파일
- `src/app/api/receipt/upload/route.ts`
- `src/app/test-ocr/page.tsx` **신규**
- `src/lib/store.ts`
- `src/app/api/db/transactions/route.ts`
- `src/app/money/page.tsx`

### 빌드 결과
- ✅ 20페이지 빌드 성공, 오류 0개
- ✅ http://localhost:4455 정상 실행
- ✅ caddy-pink.vercel.app 배포 완료 (push: `fc02525`, `53db35b`)

---

## ✅ 16회차 — PWA 아이콘 교체 + 카톡 브라우저 감지 + 설치 배너

> 날짜: 2026-04-05

### 구현 내용

#### `public/icon-192.png`, `public/icon-512.png` 교체
- 원본: `pic/20260405_110739.png` (골프카트 + 초록 원형 배경, 322×303)
- PowerShell .NET `System.Drawing` 으로 비흰색 픽셀 범위 탐색 → 정사각형 크롭
- High-Quality Bicubic 보간으로 512px / 192px 리사이즈 저장

#### `public/manifest.json` 업데이트
```json
{
  "name": "캐디매니저 Pro+",
  "short_name": "캐디Pro+",
  "theme_color": "#EC4899",
  "background_color": "#FFFFFF",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "purpose": "any maskable" },
    { "src": "/icon-512.png", "sizes": "512x512", "purpose": "any maskable" }
  ]
}
```

#### `src/components/layout/ClientLayout.tsx` — 카톡 감지 + PWA 설치 배너
- 카카오톡 / LINE 인앱브라우저 UA 감지 (`/kakaotalk/i`, `/line\//i`)
- Android / iOS 분기:
  - Android KakaoTalk: `[⋮] → 다른 브라우저로 열기` 안내
  - iOS KakaoTalk: `[···] → Safari로 열기` 안내
- `beforeinstallprompt` 이벤트 캡처 → Android Chrome 설치 배너
  - `deferredPrompt.current.prompt()` → 수락 시 자동 숨김
  - 닫기 시 `pwa_install_dismissed` localStorage 저장 (하루 1회 억제)
- iOS Safari 판별 (`/safari/i` + 크로스 브라우저 제외) → 공유 아이콘 안내
- 이미 설치된 경우(`display-mode: standalone` 또는 `navigator.standalone`) 배너 미표시

### 배너 정책
| 상황 | 배너 색상 | 내용 |
|------|----------|------|
| 카톡 인앱브라우저 | 초록 `bg-emerald-600` | 외부 브라우저로 열기 안내 |
| Android Chrome (비설치) | 핑크 `bg-pink-600` | [설치하기] 버튼 |
| iOS Safari (비설치) | 핑크 `bg-pink-600` | 공유→홈화면 추가 안내 |
| 이미 설치됨 / dismissed | — | 배너 없음 |

### localStorage 키 추가
| 키 | 용도 |
|----|------|
| `pwa_install_dismissed` | 설치 배너 하루 억제 (`toDateString()`) |

### 수정 파일
- `public/icon-192.png` (신규 교체)
- `public/icon-512.png` (신규 교체)
- `public/manifest.json`
- `src/components/layout/ClientLayout.tsx`

### 부산물 파일
- `PWA_카톡브라우저_패턴.md` — 다른 프로젝트 재사용을 위한 구현 패턴 정리

### 빌드 결과
- ✅ TypeScript 오류 0개 (`npx tsc --noEmit`)

---

## ✅ 38회차 — 현금영수증 발행 기록 DB 저장 + 목록 표시

> 날짜: 2026년 4월 7일

### 구현 내용

**Supabase 테이블 신설: `aone_pro_caddypro_cash_receipts`**
- 필드: id, dealer_token, dealer_name, amount, type, identifier_masked, order_name, issue_id, receipt_url, created_at
- RLS: ALL USING(true)
- 식별번호는 끝 4자리만 저장 (마스킹 처리)

**`src/app/api/dealer/cash-receipt/route.ts`**
- 발행 성공 시 Supabase에 기록 자동 저장

**`src/app/dealer/[token]/dashboard/page.tsx`**
- `receiptHistory`, `receiptHistoryLoading`, `loadReceiptHistory` 상태/함수 추가
- 현금영수증 탭 진입 시 자동 목록 로드
- 발행 성공 시 목록 자동 갱신
- 발행 내역 카드: 소득공제/지출증빙 뱃지, 마스킹 번호, 금액, 날짜, 영수증 확인 링크

### 수정/생성 파일
- `src/app/api/dealer/cash-receipt/route.ts`
- `src/app/dealer/[token]/dashboard/page.tsx`

### 빌드 결과
- ✅ TypeScript 오류 0개

---

## ✅ 37회차 — 딜러 대시보드 현금영수증 발행 탭 추가

> 날짜: 2026년 4월 7일

### 구현 내용

**딜러가 고객에게 직접 현금영수증 발행 가능 (PortOne V2 standalone 발행)**

**`src/app/api/dealer/cash-receipt/route.ts`** (신규)
- `POST /api/dealer/cash-receipt`
- 딜러 토큰 검증 → PortOne V2 `/cash-receipts` API 호출
- 소득공제용 (PERSONAL, 전화번호) / 지출증빙용 (CORPORATE, 사업자번호) 모두 지원
- 식별번호 자릿수 검증 포함 (전화번호 10~11자리, 사업자번호 10자리)
- 발행 실패해도 에러 메세지 반환 (서비스 중단 없음)

**`src/app/dealer/[token]/dashboard/page.tsx`** (수정)
- `DealerTab` 타입에 `'receipt'` 추가
- 현금영수증 관련 상태 4개 추가: `receiptType`, `receiptIdentifier`, `receiptAmount`, `receiptOrderName`, `isIssuingReceipt`, `receiptResult`
- `handleIssueReceipt()` 핸들러 추가
- 탭 바에 **현금영수증** 탭 추가 (BadgeCheck 아이콘)
- 현금영수증 탭 UI:
  - 소득공제용 / 지출증빙용 토글
  - 전화번호 자동 하이픈 / 사업자번호 자동 포맷팅
  - 금액 자동 콤마 포맷팅
  - 상품명 입력 (기본값: Caddy Manager Pro 이용권)
  - 발행 결과 (성공/실패 + 영수증 확인 URL 링크)

### 현금영수증 발행 흐름
```
딜러 대시보드 → [현금영수증] 탭
  → 소득공제 / 지출증빙 선택
  → 고객 전화번호 or 사업자번호 입력
  → 금액 입력
  → [현금영수증 발행] 버튼
  → 서버 → PortOne V2 /cash-receipts API
  → 국세청 자동 전송 ✅
```

### 수정/생성 파일
- `src/app/api/dealer/cash-receipt/route.ts` (신규)
- `src/app/dealer/[token]/dashboard/page.tsx`

### 빌드 결과
- ✅ TypeScript 오류 0개

---

## ✅ 36회차 — 실시간계좌이체 현금영수증 자진발급 + 딜러 세무처리 기획문서

> 날짜: 2026년 4월 7일

### 구현 내용

**`src/app/api/payment/complete/route.ts`**
- 실시간계좌이체(Transfer) 결제 완료 시 현금영수증 자동 처리 로직 추가
- 조건: 결제수단이 계좌이체 AND 고객이 영수증 미선택 AND 금액 10만원 이상
- → 포트원 `POST /payments/{id}/cash-receipts` API 호출로 국세청 자진발급 자동 처리
- 현금영수증 발급 실패해도 이용권 발급은 정상 진행 (try-catch 분리)

### 현금영수증 처리 케이스 정리
| 상황 | 처리 |
|------|------|
| 고객이 번호 입력하고 결제 | 포트원 자동 발급 (hasReceipt=true → 스킵) |
| 고객 건너뜀 + 10만원↑ | 자진발급 로직 자동 실행 ✅ |
| 카드결제 | isTransfer=false → 스킵 (카드사 자동 처리) |
| 10만원 미만 계좌이체 | 법적 의무 없음 → 스킵 ✅ |

### 기획문서 생성
- `html/기획_딜러_영수증_세무처리.html` — 딜러 결제·영수증·세무처리 구조 기획서
  - 안 A (사업자 딜러): 세금계산서 교환 구조 + 실무 5단계
  - 안 B (프리랜서 3.3%): 원천징수 구조 + 원천징수 계산 예시 + 실무 6단계
  - A vs B 비교표 + 결론 (병행 채택 권장)
  - 딜러 시스템 외부 수금 대처 방안 3가지 (계약서/선충전구조/채널통제)

### 수정 파일
- `src/app/api/payment/complete/route.ts`
- `html/기획_딜러_영수증_세무처리.html`

---

## ✅ 35회차 — 크레딧 구매 장바구니 테스트 페이지 (안 C)

### 구현 내용
- `/test-pay` 페이지를 **장바구니(Cart) 방식** 다중 크레딧 구매 테스트 페이지로 완전 교체
- 스탠다드/프리미엄 × 1개월/6개월/1년 × 수량(최대 20장) 자유 조합
- 상품 선택 후 "담기" → 장바구니 적재 → "모의 결제" → 코드 일괄 발급
- 마스터 비번(0827) PIN 게이트로 외부 접근 방지
- 랜딩 페이지 맨 아래 "업데이트 평생 무료" 링크(→ `/test-pay`) 통해 진입

### 생성/수정 파일
- `src/app/test-pay/page.tsx` — 전면 재작성 (장바구니 Cart UI)
- `src/app/api/test/issue-cart/route.ts` — 신규 (다중 코드 일괄 발급 API, 최대 50장)

### 페이지 플로우
1. PIN 입력(0827) → 인증
2. 장바구니 화면: 현재 담긴 항목 그리드(6칸) + 구매자 정보 + 상품 선택 + 담기 버튼
3. 담긴 항목이 있으면 "총 N장 모의 결제" 버튼 활성화
4. 결제 클릭 → `/api/test/issue-cart` 호출 → 코드 일괄 발급
5. 결과 화면: 발급된 코드 목록 + 개별/전체 복사 버튼

---

## ✅ 34회차 — 인원관리: 고객관리/캐디관리 탭 분리

> 날짜: 2026년 4월  
> 커밋: `fac451d`

### 구현 내용

#### Supabase: `aone_pro_caddypro_caddies` 테이블 신설
- 필드: id, license_code, name, caddy_no, group_no, rank(조장/일반/대기), contact, memo, is_active, created_at
- RLS: ALL USING(true)

#### `/api/db/caddies/route.ts` (신규)
- **GET**: license_code 기준 캐디 목록 (group_no/caddy_no 정렬)
- **POST**: 새 캐디 등록

#### `/api/db/caddies/[id]/route.ts` (신규)
- **PUT**: 캐디 정보 수정
- **DELETE**: 캐디 삭제

#### `store.ts` 확장
- `CaddyRank`, `Caddy` 인터페이스 추가
- `caddies: Caddy[]` 상태
- `addCaddy() / updateCaddy() / deleteCaddy()` 액션
- `initializeStore`: Promise.all 6개 (caddies 추가)
- `resetData`: caddies 초기화 포함

#### `BottomNav.tsx`
- "고객" → "인원관리" 레이블 변경

#### `clients/page.tsx` 전면 개편
- **상단 탭**: 고객관리 | 캐디관리 (PageTab 타입)
- **고객관리 탭**: 기존 기능 유지 (VIP/일반/진상 필터 + 등록/수정/삭제)
- **캐디관리 탭**:
  - 조별 탭 필터 (전체/1조~6조) + 조원 수 뱃지
  - 캐디 카드: 직위 왼쪽 컬러보더(조장=amber/대기=sky/일반=stone), 이름·직위뱃지·조번호·캐디번호·연락처
  - 등록 폼: 이름, 직위(조장/일반/대기), 조번호, 캐디번호, 연락처, 메모
  - 수정/삭제 기능

### 수정/생성 파일
- `src/app/api/db/caddies/route.ts` — NEW
- `src/app/api/db/caddies/[id]/route.ts` — NEW
- `src/lib/store.ts` — Caddy 타입/액션/initializeStore 업데이트
- `src/components/layout/BottomNav.tsx` — "인원관리" 레이블
- `src/app/clients/page.tsx` — 고객관리/캐디관리 탭 분리

### 빌드 결과
- ✅ TypeScript/JSX 에러 0개 (clients/page.tsx)
- ✅ `git push new_origin master` 배포 완료

---

## ✅ 33회차 — pattern 에러 수정 (콤마포함 숫자)

> 날짜: 2026년 4월  
> 커밋: `52ffe2f`

### 구현 내용
- **문제**: `pattern="[0-9]*"` + 콤마 포함 값(`20,000`) → 브라우저 form validation 실패
- **해결**: `pattern` 속성 제거, `inputMode="numeric"`만 유지
- **파일**: `src/app/schedule/page.tsx` (오버피 input)

---

## ✅ 32회차 — 팁→overFee 단일화, 모달 중앙팝업, "오늘의 메모"

> 날짜: 2026년 4월  
> 커밋: `4e3306b`

### 구현 내용
- **팁 이중저장 문제 해결**: 팁 → `schedule.overFee`에만 저장 (diary 중복 저장 제거)
- **모달**: 바텀시트 → 중앙 팝업 (`items-center`)
- **레이블**: "메모 (일지)" → "오늘의 메모"
- **파일**: `src/app/home/page.tsx`, `src/app/money/page.tsx`

---

## ✅ 31회차 — diary 수입일지 + 월간일지 탭

> 날짜: 2026년 4월  
> 커밋: `0353895`

### 구현 내용

#### Supabase: `aone_pro_caddypro_diary` 테이블 신설
- `UNIQUE(license_code, date)` → UPSERT 기반 (날짜당 1레코드)
- 필드: caddy_fee_1/2/3 (스케줄 자동), tip_1/2/3 (직접입력), extra_reason/amount (기타), memo (일지)
- RLS: ALL USING(true) / 자동 updated_at 트리거

#### `/api/db/diary` 라우트 (신규)
- **GET**: license_code 기준 전체 일지 조회 (날짜 내림차순)
- **POST**: UPSERT (onConflict: `license_code,date`)

#### `store.ts` 확장
- `Diary` 인터페이스 추가
- `diaries: Diary[]` 상태 추가
- `upsertDiary()` — POST /api/db/diary 호출 + 로컬 상태 즉시 반영
- `getDiaryByDate(date)` — 로컬 조회 헬퍼
- `initializeStore`: Promise.all 5개로 확장 (diary 포함)

#### `home/page.tsx` — 수입입력 모달 전면 교체
- 구: 캐디피/팁/오버피 단일 입력 → `addTransaction()` 호출
- 신: 오늘 수입 일지 (1부/2부/3부 캐디피 readonly 자동 + 팁 직접입력 + 기타수입사유/금액 + 메모) → `upsertDiary()` 호출
- 모달 열릴 때 기존 diary 데이터 자동 불러오기 (수정 지원)

#### `money/page.tsx` — 월간일지 탭 추가
- 상단 탭 3개: **가계부** | **월간일지** | **세금**
- 월간일지 탭: 현재 달 diary 레코드 날짜별 카드 표시
  - 캐디피(부별) / 팁(부별) / 기타수입 / 메모
  - 빈 달 안내 메시지

### 수정/생성 파일
- `src/app/api/db/diary/route.ts` — NEW
- `src/lib/store.ts` — Diary 타입/액션/initializeStore 업데이트
- `src/app/home/page.tsx` — 수입입력 모달 전면 교체
- `src/app/money/page.tsx` — 월간일지 탭 추가
- `supabase_diary_table.sql` — DDL 파일 (MCP로 실행 완료)

---

## ✅ 30회차 — 이용권 UI 개선 + /test-pay 발급 페이지

> 날짜: 2026년 4월  

### 구현 내용
- **이용권 플랜 UI**: 월 환산가 + 할인율 배지 표시 (landing, subscribe)
- **/test-pay**: 결제 없이 이용권 코드 발급 (PIN 0827, 랜딩 하단 링크)
- **LicenseGuard 예외**: `/test-pay` 접근 허용 (ClientLayout.tsx)
- **결제 금액 원복**: 150원 테스트 → 운영가격으로 복원

### 수정 파일
- `src/app/landing/page.tsx`, `src/app/subscribe/page.tsx`
- `src/app/test-pay/page.tsx` (NEW)
- `src/app/api/test/issue/route.ts` (NEW)
- `src/components/layout/ClientLayout.tsx`

---

## ✅ 28회차 — /change 페이지 검색 + 감사금액(원화)/날짜 + 지출 연동

> 날짜: 2026년 4월  
> 커밋: `f604560`

### 구현 내용

#### DB 스키마 추가
- `swap_thanks_amount` (integer, DEFAULT 0) — 이미 존재
- `swap_thanks_date` (date, DEFAULT NULL) — 신규 추가

#### 검색 기능
- 상단 검색창: 이름 / 메모 / 감사내용 통합 검색
- 이름 통계 뱃지 터치 → 해당 이름 자동 검색 필터
- 검색 중엔 연도/월 필터 숨김

#### 감사 표시 (감사 버튼 → 새 폼)
- **감사 내용** 텍스트 입력 (기존 기능 유지)
- **감사 금액** 입력 (예: `222,222` 원화 포맷 자동 적용)
- **날짜** 선택 (date picker, 기본값: 오늘)
- 금액 > 0 저장 시 → "가계부에 지출 추가?" 팝업
  - [추가] → addTransaction으로 지출 자동 등록
  - [메모만] → 지출 없이 감사기록만 저장
- 감사 버튼: 기존 감사내용 + 금액/날짜 간략 표시

### 수정 파일
- `src/lib/store.ts` — swapThanksAmount, swapThanksDate 타입 추가
- `src/app/api/db/schedules/route.ts` — GET/POST 매핑 추가
- `src/app/api/db/schedules/[id]/route.ts` — PUT 매핑 추가
- `src/app/change/page.tsx` — 전체 교체

### 환경
- DB: `swap_thanks_date` 컬럼 Supabase에서 직접 추가

---

## ✅ 27회차 — /change 페이지 풀 CRUD + 연도별/월별 필터

> 날짜: 2026-04-06  
> 커밋: `419ae06`

### 구현 내용

#### 연도별 + 월별 2단계 필터
- 상단 탭: **전체** / **2026년** / **2025년** / …  
- 연도 선택 시 하위 탭: **전체** / **4월** / **3월** / …  
- 기록이 없는 현재 연도도 자동 포함 (과거 기록 추가 가능)

#### 인라인 수정/삭제
- 각 카드 우상단 ✏️ 버튼 → 이름·메모 필드 인라인 편집 → [저장] [취소]
- 🗑️ 버튼 → confirm → swap_with/memo/thanks 컬럼 null 처리 (schedule 레코드 유지)

#### 신규 추가 폼 (+ 추가 버튼)
- 날짜 picker + 부(1/2/3) + 이름(필수) + 메모(선택)
- 해당 날짜+부에 work 스케줄 있으면 → updateSchedule로 swap 정보 추가
- 없으면 → addSchedule로 새 work 스케줄 생성 (type:'work')

#### 이름별 통계 — 필터 연동
- 선택된 연도/월 기준으로 집계 자동 갱신

### 수정 파일
- `src/app/change/page.tsx` — 전체 교체

---

## ✅ 26회차 — 날짜 버그 수정 + 홀별 통계 + 투라운드 묶음 카드

> 날짜: 2026-04-06

### 1. 오전 9시 전 날짜 오인식 버그 수정

**원인:** `new Date().toISOString().split('T')[0]` → UTC 기준이라 한국 오전 9시 전은 전날로 인식  
**해결:** `utils.ts`에 `todayKST()` 함수 추가 (UTC+9 기준 날짜 반환)

```typescript
// src/lib/utils.ts
export function todayKST(): string {
    return new Date(Date.now() + 9 * 60 * 60 * 1000).toISOString().split('T')[0];
}
```

**영향:** 오전 9시 이전 당일 근무가 "예상합계"로 잡히던 문제 → 즉시 "실제수익"으로 반영

### 2. 홀별 근무 횟수 통계 추가 (홈 카드 + 연간현황)

**홈 카드 (당월):**
- 18홀 → 항상 표시
- 36홀 / 54홀 / 9홀 / 기타 → 데이터 있을 때만 표시
- 예상치(미래) `+N` 뱃지 동일 적용

**연간현황 카드:**
- `grid-cols-3` 고정 → `flex` 방식으로 변경
- 홀 종류가 늘어나도 자동으로 컬럼 추가

### 3. 수입내역 날짜별 섹션 + 투라운드 묶음 카드

**기존:**
```
4월 6일 (월)  근무(완료) 자동  +150,000  ← 1부
4월 6일 (월)  근무(완료) 자동  +150,000  ← 2부
```

**변경 후:**
```
── 4월 6일 (월) ──────────── +300,000
  🏌️ 36홀(투) 근무  자동   합계 +300,000
     1부  18홀  근무완료   +150,000
     2부  18홀  근무완료   +150,000
```

- 같은 날 work 2건 이상 → 묶음 카드 (터치 시 1부/2부 상세 표시)
- 1건 단독 → 기존 단일 카드 유지
- 날짜별 섹션 헤더 + 당일 수입/지출 소계 표시

### 4. 3월 10일 휴무+근무 중복 데이터 삭제

```sql
DELETE FROM aone_pro_caddypro_schedules
WHERE date = '2026-03-10' AND type = 'work'
  AND license_code IN ('DC-AWA-S72', 'DL-B8A-S72');
```
- 휴무일에 근무가 함께 저장된 오염 데이터 제거
- DC-AWA-S72, DL-B8A-S72 모두 holiday + personal 2건만 남음

### 5. 전체 Supabase 데이터 재복원

`caddy-backup-2026-04-05.json` 기준으로 두 이용권 완전 복원:

| 테이블 | DC-AWA-S72 (윤희라) | DL-B8A-S72 (이현석) |
|--------|------|------|
| schedules | 118건 ✅ (→3/10 work 삭제 후 117건) | 118건 ✅ (→117건) |
| transactions | 37건 ✅ | 37건 ✅ |
| clients | 1건 ✅ | 1건 ✅ |

### 수정 파일
- `src/lib/utils.ts` — `todayKST()` 함수 추가
- `src/app/home/page.tsx` — todayKST 적용 + 홀별 통계 36/54홀 추가 + 연간현황 flex 변경
- `src/app/money/page.tsx` — todayKST 적용 + 날짜 섹션 헤더 + 투라운드 묶음 카드
- `src/app/schedule/page.tsx` — todayKST 적용
- `src/app/tax/page.tsx` — todayKST 적용
- `src/app/settings/page.tsx` — todayKST 적용

---

## 📋 현재 미완료 / 다음 작업 후보

### 즉시 가능
- [ ] Vercel 배포 (new_origin → caddy_pro_enterprise.git)
- [ ] Vercel 환경변수: `GEMINI_API_KEY` 등록 → OCR 즉시 활성화
- [ ] 이용권 만료 전 자동 알림

### 중기 계획
- [ ] 이용권 만료 후 90일 R2 데이터 보존 → 재구매 시 자동 연결
- [ ] 딜러 정산 요청 알림 (카카오 알림톡 or 이메일)
- [ ] 도메인 구매: caddypro.kr

---

## ✅ 25회차 — Supabase 데이터 복원 + 캐시 우선 로드 제거

### 작업 배경
- 윤희라(DC-AWA-S72) Supabase 데이터 0건 — 복원 필요
- 이현석(DL-B8A-S72) 3월 수입 11,340,000원 이상으로 계산 — transactions 중복 데이터 원인
- 캐시 우선 로드로 인해 다른 이용권 데이터가 화면에 노출 가능한 구조적 문제

### 완료된 작업

#### 1. store.ts 캐시 우선 로드 제거
- `src/lib/store.ts` — `initializeStore()` 함수 1단계 캐시 우선 로드 블록 완전 제거
- Supabase가 항상 source of truth (로컬 캐시는 폴백용으로만 유지)
- 이용권 코드 전환 시 이전 유저 데이터가 화면에 노출되는 문제 해결

#### 2. 윤희라(DC-AWA-S72) Supabase 복원
- schedules: **117건** INSERT (3/10 work 제외, gen-sql-yunheera.mjs로 생성)
- transactions: **37건** INSERT (insert_transactions.sql 파일 기준 실제 데이터)
- clients: **1건** INSERT (서두리, 아우디 27가3209)

#### 3. 이현석(DL-B8A-S72) transactions 삭제
- `DELETE FROM aone_pro_caddypro_transactions WHERE license_code = 'DL-B8A-S72'`
- schedules.caddy_fee + transactions.income 중복 계산 문제 해결

### 최종 검증 결과

| 테이블 | 이용권 | 건수 |
|--------|--------|------|
| schedules | DC-AWA-S72 | 117 ✅ |
| schedules | DL-B8A-S72 | 116 ✅ |
| schedules | DL-NG9-WRQ | 18 ✅ |
| transactions | DC-AWA-S72 | 37 ✅ |
| transactions | DL-B8A-S72 | 0 (삭제됨) ✅ |
| clients | DC-AWA-S72 | 1 ✅ |
| clients | DL-B8A-S72 | 1 ✅ |

### 수정 파일
- `src/lib/store.ts` — 캐시 우선 로드 블록 제거

### 생성 파일
- `gen-sql-yunheera.mjs` — DC-AWA-S72 SQL 생성 스크립트
- `insert_schedules.sql` — DC-AWA-S72 schedules 117건
- `insert_transactions.sql` — DC-AWA-S72 transactions 37건
- `insert_clients.sql` — DC-AWA-S72 clients 1건

---

## 📋 현재 미완료 / 다음 작업 후보

### 즉시 가능
- [ ] Vercel 환경변수: `GEMINI_API_KEY` 등록 → OCR 즉시 활성화
- [ ] 이용권 만료 전 자동 알림
- [ ] subscribe 페이지: `payMethod` URL 파라미터 분기

### 중기 계획
- [ ] 이용권 만료 후 90일 R2 데이터 보존 → 재구매 시 자동 연결
- [ ] 딜러 정산 요청 알림 (카카오 알림톡 or 이메일)
- [ ] 흑백 WebP 변환 (선택적, 공수 2시간)
- [ ] 도메인 구매: caddypro.kr

---

## ✅ 17회차 — localStorage → Supabase 완전 전환 (안 A)

> 날짜: 2026-04-05  
> 커밋: `b904965`

### 핵심 결정
- localStorage + 하이브리드 → **Supabase 완전 전환 (안 A)**
- Zustand persist 완전 제거, 순수 메모리 + bgFetch API 방식

### 구현 내용

#### `src/lib/supabaseServer.ts` **신규**
- 서버 전용 클라이언트 (Service Role Key 우선)

#### `src/app/api/db/*` **신규 8개 Route**
| Route | Methods | 기능 |
|-------|---------|------|
| `/api/db/schedules` | GET, POST | 일정 전체 조회/추가 |
| `/api/db/schedules/[id]` | PUT, DELETE | 일정 수정/삭제 |
| `/api/db/clients` | GET, POST | 고객 전체 조회/추가 |
| `/api/db/clients/[id]` | PUT, DELETE | 고객 수정/삭제 |
| `/api/db/transactions` | GET, POST | 거래 전체 조회/추가 |
| `/api/db/transactions/[id]` | DELETE | 거래 삭제 |
| `/api/db/fee-settings` | GET, PUT | 캐디피 설정 |
| `/api/db/migrate` | POST | 로컬→Supabase 일괄 이전 |

모든 Route: `x-license-code` 헤더 인증, 자동 UPPER()

#### `src/lib/store.ts` 전면 재작성
- `persist` import/wrapper 완전 제거
- 모든 CRUD 액션: 메모리 즉시 반영 + `bgFetch` API 호출
- `initializeStore(licenseCode)` 함수 추가 (앱 시작 시 Supabase에서 전체 로딩)

#### `src/components/layout/LicenseGuard.tsx`
- `initializeStore()` 호출 연결
- 무한루프 버그 수정: `router.replace('/landing')` 잘못된 위치 제거

#### `src/components/layout/ClientLayout.tsx` 경로 분기
- 랜딩/구독/딜러 → 전체폭 그대로
- 앱 내부 → `max-w-[480px]` 컨테이너 + BottomNav

#### `src/app/layout.tsx`
- 컨테이너 div 제거, viewport `device-width`로 복원 (랜딩 페이지 깨짐 수정)

#### `src/app/landing/layout.tsx` **신규**
- 빈 pass-through (랜딩 레이아웃 독립)

### Supabase DB 변경
```sql
-- 모든 테이블 license_code 자동 대문자 트리거 (5개 테이블)
CREATE TRIGGER trg_upper_{table}_code BEFORE INSERT OR UPDATE ON ...

-- schedules 테이블 time 컬럼 누락 추가
ALTER TABLE aone_pro_caddypro_schedules ADD COLUMN time TEXT DEFAULT '00:00';

-- 기존 코드 대문자 일괄 통일
UPDATE aone_pro_caddypro_licenses SET code = UPPER(code);
```

### 수정 파일
- `src/lib/store.ts` (전면 재작성)
- `src/lib/supabaseServer.ts` **신규**
- `src/app/api/db/*` **신규 8개**
- `src/components/layout/LicenseGuard.tsx`
- `src/components/layout/ClientLayout.tsx`
- `src/app/layout.tsx`
- `src/app/landing/layout.tsx` **신규**

---

## ✅ 18회차 — DC-AWA-S72 마이그레이션 실행 + 실시간 CRUD 확인

> 날짜: 2026-04-05  
> 이전 커밋 연속

### 마이그레이션 실행 (윤희라 DC-AWA-S72)
1. 로컬 서버 실행 → DC-AWA-S72 로그인
2. 설정 → "지금 바로 동기화하기" 클릭
3. 첫 시도: 0건 저장 → 원인: `dc-AWA-S72` vs `DC-AWA-S72` 대소문자 불일치
4. Supabase SQL로 코드 대문자 처리 + 트리거 추가
5. **두 번째 시도 성공**

### 마이그레이션 결과
| 테이블 | 건수 |
|--------|------|
| schedules | **84건** ✅ |
| transactions | **14건** ✅ |
| clients | 0건 (원래 없음) |
| fee_settings | 있음 ✅ |

### 실시간 CRUD 확인
- 새 일정 추가 → Supabase `schedules` 즉시 저장 확인
- 2026-04-04 1부 근무 / 2부 근무 각각 즉시 반영
- 실시간 CRUD 정상 동작 ✅

---

## ✅ 19회차 — Gemini OCR 활성화 + Supabase ocr_usage 연동

> 날짜: 2026-04-05  
> 커밋: `4b2f487`

### 구현 내용

#### Supabase 테이블 신규 생성
```sql
CREATE TABLE aone_pro_caddypro_ocr_usage (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    license_code TEXT NOT NULL,
    year_month TEXT NOT NULL,
    count INTEGER NOT NULL DEFAULT 0,
    UNIQUE(license_code, year_month)
);
-- license_code 자동 대문자 트리거 추가
```

#### `/api/db/ocr-usage/route.ts` **신규**
- GET: 현재 월 사용 횟수 조회 (`?ym=YYYY-MM`)
- POST: 사용 횟수 +1 (upsert)
- 기기 변경해도 횟수 유지 (localStorage 아닌 Supabase 저장)

#### `src/app/money/page.tsx` — 마운트 시 Supabase 조회
- `useEffect` on mount → GET `/api/db/ocr-usage?ym=YYYY-MM`
- `incrementOcrCount()` → POST `/api/db/ocr-usage`
- localStorage `caddy_ocr_YYYY-MM` 카운트 방식 → Supabase 방식으로 교체

#### `.env.local` + Vercel 환경변수 등록
```
GEMINI_API_KEY=AIzaSyA7eVBuEB74Vi7Xm7m9QXKk5hyUv_-r9J0
```
- Vercel CLI로 `caddy-pink` 프로젝트에 production 환경변수 등록 완료

### OCR 동작 조건
| 조건 | 동작 |
|------|------|
| `GEMINI_API_KEY` 없음 | OCR 건너뜀, URL만 저장 |
| `GEMINI_API_KEY` 있음 + 이미지 < 4MB | Gemini 1.5 Flash로 금액/상호 추출 |
| plan = month | 월 5회 제한 |
| plan = 6month | 월 30회 제한 |
| plan = year | 무제한 |

### 수정 파일
- `.env.local` (GEMINI_API_KEY 추가)
- `src/app/api/db/ocr-usage/route.ts` **신규**
- Supabase: `aone_pro_caddypro_ocr_usage` 테이블 + 트리거

### 빌드 결과
- ✅ Next.js 빌드 성공
- ✅ `git push new_origin master` → Vercel 배포 완료
- ✅ OCR 즉시 활성화 (배포 후 사진 찍으면 금액 자동 추출)

---

## ✅ 23회차 — OCR 업로드 타이밍 수정 (등록 전 R2 저장 방지)

> 날짜: 2026-04-05

### 문제
- 영수증 사진 첨부 순간 R2에 이미지 업로드됨
- 지출등록하기를 누르지 않은 고아 파일(orphan file)이 R2에 쌓이는 구조

### 해결 (안 A: 클라이언트 state 보관)
- 파일 선택 → OCR만 수행(`/api/receipt/ocr`) → `receiptFile` state에 File 보관
- 지출등록하기 클릭 → R2 업로드 → DB 저장 순서로 변경

### 데이터 흐름 (변경 후)
```
사진 첨부 → canvas 압축 → receiptFile(state) + URL.createObjectURL(미리보기)
  ↓
/api/receipt/ocr (OCR만, R2 저장 없음)
  ↓
금액/상호명 자동입력
  ↓
[지출등록하기] 클릭
  ↓
/api/receipt/upload (skipOcr=true) → R2 저장
  ↓
addTransaction (DB 저장)
```

### 생성/수정 파일
- `src/app/api/receipt/ocr/route.ts` **신규** — Gemini OCR만, R2 저장 없음
- `src/app/api/receipt/upload/route.ts` — `skipOcr` 파라미터 추가
- `src/app/money/page.tsx` — `receiptFile` state 추가, `handleReceiptChange` / `handleSubmit` 분리

---

## ✅ 24회차 — 딜러 크레딧(토큰) 시스템 구현

### 개요
관리자가 딜러에게 이용권 크레딧을 충전하면, 딜러가 현장에서 결제 없이 즉시 이용권을 발급할 수 있는 시스템.

### 확정 공급가
| 플랜 | 스탠다드 소비자가 | **딜러 공급가** | 프리미엄 소비자가 | **딜러 공급가** |
|------|------|------|------|------|
| 1개월 | 9,900원 | **7,000원** | 12,900원 | **10,000원** |
| 6개월 | 55,000원 | **40,000원** | 69,000원 | **50,000원** |
| 1년 | 99,000원 | **70,000원** | 129,000원 | **100,000원** |

### DB 변경
```sql
-- dealers 테이블 컬럼 추가
credits_month INT DEFAULT 0
credits_6month INT DEFAULT 0
credits_year INT DEFAULT 0
credits_month_premium INT DEFAULT 0
credits_6month_premium INT DEFAULT 0
credits_year_premium INT DEFAULT 0

-- 신규 테이블
aone_pro_caddypro_dealer_credit_history
  (id, dealer_id, type, plan, tier, qty, memo, created_at)
```

### 구현 내용
**관리자 페이지 (`src/app/admin/page.tsx`)**
- 딜러 카드에 크레딧 잔량 6개 뱃지 표시 (스탠다드/프리미엄 × 1개월/6개월/1년)
- "충전" 버튼 클릭 → 모달: 티어/플랜 선택 + 수량 입력 + 공급가 표시 → 즉시 충전
- `aone_pro_caddypro_dealer_credit_history`에 'charge' 이력 기록

**딜러 대시보드 (`src/app/dealer/[token]/dashboard/page.tsx`)**
- 헤더에 크레딧 잔량 뱃지 (0 초과인 플랜만 표시)
- 신규발급 탭에서 선택한 플랜/티어 크레딧 있으면 "💳 크레딧 발급(`n`장)" 버튼 표시
- 크레딧 발급 시: issueVoucher → credits 차감 → history 'use' 기록
- 고객 목록에서 pay_method='credit' 표시

### 생성/수정 파일
- `src/app/admin/page.tsx` — 크레딧 충전 모달 + 딜러 카드 개선
- `src/app/dealer/[token]/dashboard/page.tsx` — 크레딧 잔량 표시 + 크레딧 발급 버튼

---

## 26회차 — 딜러 크레딧 결제 무한루프 버그 수정 (2025)

### 커밋: `39e9fe5`

### 문제
KG이니시스 계좌이체가 **리다이렉트** 방식으로 동작할 때:  
- 결제창 → 은행 페이지 이동 → dealer dashboard로 복귀  
- `requestPayment` Promise가 resolve되지 않아 `caddy_credit_purchase`가 sessionStorage에 잔류  
- 읽는 코드가 없어 결제가 완료됐어도 처리되지 않음 → 사용자가 버튼을 재클릭 → 루프

### 수정 내용

**`src/app/dealer/[token]/dashboard/page.tsx`**
1. **`visibilitychange` 핸들러** 추가: 팝업 닫히거나 페이지 복귀 시 `setIsBuyingCredit(false)` 실행
2. **리다이렉트 복구 useEffect** 추가 (마운트 시):  
   - `sessionStorage.getItem('caddy_credit_purchase')` 읽기  
   - dealerToken 일치 확인 → `/api/dealer/credit/charge` POST 호출  
   - 성공 시 `setCreditBuyResult('success')` + `loadDealer()` 새로고침  
   - sessionStorage 즉시 삭제 (중복 처리 방지는 API 서버에서도 보호)
3. **`redirectUrl` 추가**: `requestPayment` 옵션에 현재 딜러 대시보드 URL 지정

---

## 27회차 — 딜러 크레딧 구매 UI 개선 (장바구니 + 할인 표시) (2026-04-05)

### 커밋: `0dd68f6`

### 구현 내용

**`src/app/dealer/[token]/dashboard/page.tsx`**

1. **소비자가 상수 추가** (`CONSUMER_PRICE`)
   - standard: 9,900 / 55,000 / 99,000
   - premium: 12,900 / 69,000 / 129,000

2. **기간 버튼 개선**
   - 소비자가 취소선으로 표시
   - 공급가(내 구매가) 초록색 표시
   - 할인율 배지 (빨간 %, 예: "29% 할인")

3. **장바구니 시스템** (`CartItem` 타입, `cart` state)
   - 종류/기간/수량 선택 → 🛒 담기 버튼
   - 동일 항목 담기 시 수량 합산
   - 담은 후 장바구니 섹션 표시

4. **장바구니 섹션 UI**
   - 항목별: `~~소비자가~~` → `내구매가` + `(N원 절약)` 표시
   - 합계: 소비자가 합계 / 내 구매가 / 절약 금액 🎉 / 할인율
   - 결제수단 선택 + 결제 버튼 (장바구니 총액 표시)

5. **결제 버튼** — 장바구니에 항목이 있을 때만 표시
   - "₩XXX,XXX 결제하기" 형식으로 총액 직접 노출

**`src/app/api/dealer/credit/charge/route.ts`**
- `cartItems: [{plan, tier, qty}]` 배열 처리 추가
- 단 한 번의 결제로 여러 플랜 크레딧 일괄 충전
- `updateMap`으로 DB 칼럼 한 번에 업데이트
- 이력은 항목별로 개별 기록
- 단건 구버전 형식 호환 유지 (`plan/tier/qty` → `cartItems` 변환)

**`src/app/landing/page.tsx`**
- 헤더에 "딜러 입력" 버튼 추가 (황금색 테두리) → `/dealer-login` 이동

### 기타 (같은 회차)
- 결제 취소/성공 → 팝업 모달로 변경 (인라인 알림 제거, 커밋 `f215f09`)
- 딜러 크레딧 무한루프 버그 수정 (커밋 `39e9fe5`)

---

## 28회차 — 백업 구조 v2 날짜 KST + ownerName 추가 (2026-04-05)

### 수정 파일

**`src/lib/store.ts`**
- `exportData()`: `exportedAt` UTC → KST(`+09:00`)로 수정
- `ownerName` 필드 추가 (localStorage `caddy_user_name_<code>` 우선, fallback `caddy_user_name`)
- `version: 2` 유지

**`src/lib/r2Client.ts`**
- `backedUpAt` UTC → KST(`+09:00`)로 수정
- `today` 날짜도 KST 기준 `YYYY-MM-DD` 사용 (자정 넘김 기준 올바른 날짜)

### 비고
- R2 일별 스냅샷(`YYYY-MM-DD.json`) 이미 구현됨 ✅
- `transactions` / `schedules` 원래부터 분리된 테이블 ✅ — isManual 플래그 불필요

---

## 📋 현재 미완료 / 다음 작업 후보

- [ ] 배포 서버에서 DC-AWA-S72 로그인 → 84건 로딩 + OCR 동작 확인
- [ ] 로그아웃 시 Zustand store 초기화 (메모리 잔류 방지)
- [ ] 다른 사용자 마이그레이션 (김경준 DL-NG9-WRQ 등)
- [ ] 이용권 만료 전 자동 알림
- [ ] 도메인 구매: caddypro.kr
- [ ] **영수증 사진 달별/분기별 ZIP 다운로드** — DB `transactions.date` 기준 필터 → `jszip`으로 묶어서 다운로드

---

## 🔄 17회차

### 완료 작업

#### 딜러 수수료: 고객 직접결제 시 자동 연결 + settlements 적립 ✅

**문제**: 
- `subscribe/page.tsx`가 URL `?ref=딜러토큰`을 캡처하지만 결제 API로 전달하지 않음
- `payment/complete/route.ts`에서 `issuedBy: 'portone'` 하드코딩 → 딜러 수수료 미기록
- admin 페이지가 `aone_pro_caddypro_dealer_settlements` (잘못된 테이블명) 사용

**수정된 파일**:
- `src/lib/paymentService.ts` — `dealerRef?: string` 파라미터 추가, sessionStorage에 포함
- `src/app/subscribe/page.tsx` — `requestCaddyPayment()`에 `dealerRef` 전달, PC/모바일 양쪽 fetch에 포함
- `src/app/api/payment/complete/route.ts` — 딜러 조회 로직 추가, `issuedBy` 동적 결정, settlements 적립
- `src/app/admin/page.tsx` — 테이블명 `dealer_settlements` → `settlements` 통일
- `html/기획_티어변경_경우의수_대처방안.html` — 수수료 구조 섹션 추가

**로직 요약**:
1. `dealerRef` 있음 → dealers 테이블에서 token으로 조회 → `issued_by = 'dealer_X'` + settlements `type='first'`
2. `dealerRef` 없음 → 동일 전화번호 기존 license.issued_by 역추적 → 재구독으로 수수료 적립 (`type='renewal'`)
3. 딜러 못 찾음 → `issued_by = 'portone'` (수수료 없음)
4. `commission_rate`: dealers 테이블 컬럼 우선, NULL이면 기본값 20%(0.2)
5. settlement 실패 시 이용권 발급은 유지 (try-catch 격리)

**⚠️ 필요한 Supabase 조치**:
- `aone_pro_caddypro_dealers` 테이블에 `commission_rate NUMERIC DEFAULT 0.2` 컬럼 추가 (없는 경우)
- `aone_pro_caddypro_settlements` 테이블 존재 확인 (`dealer_id`, `license_code`, `type`, `plan`, `sale_amount`, `commission_rate`, `commission_amount`, `settled`, `settled_at`, `created_at` 컬럼 필요)

---

## ✅ 39회차 — 홈 UX 개선 4종 + 가계부 미래 예정 숨김

> 날짜: 2026년 4월 8일
> 커밋: `89b2669`, `2798e62`

### 구현 내용

1. **휴무 시 팁·오버피 숨김** (home/page.tsx) — hasWork 조건으로 팁·오버피 섹션 숨김
2. **36홀 빠른 선택 버튼** (schedule/page.tsx)
3. **수입 입력 버튼 강조** (home/page.tsx) — bg-emerald-500 text-white
4. **날짜 그룹 홀수 집계** (home/page.tsx) — 같은 날 1부+2부 = 36홀
5. **가계부 미래 예정 근무 제외** (money/page.tsx) — if (isFuture) return;

---

## ✅ 40회차 — Web Push 알람 기능 구현

> 날짜: 2026년 4월 8일
> 커밋: `c60c346`

### 신규/수정 파일
- src/lib/store.ts — alarmAt, alarmSent 타입 추가
- src/app/settings/page.tsx — 알람 UI + Push 구독
- src/app/schedule/page.tsx — handleSubmit alarmAt 계산
- src/app/api/push/subscribe/route.ts **신규**
- src/app/api/cron/alarm/route.ts **신규** — 5분 Cron, web-push 발송
- worker/index.ts **신규** — SW push 이벤트 처리
- next.config.mjs — customWorkerDir: 'worker'
- vercel.json **신규** — Cron `*/5 * * * *`
- .env.local — VAPID 키 3개 + CRON_SECRET 추가
- supabase_push_alarm.sql **신규** — Supabase에서 직접 실행 필요

### ⚠️ 배포 후 수동 작업 필요

**1. Supabase SQL Editor에서 supabase_push_alarm.sql 실행**

**2. Vercel 대시보드 환경변수 추가**:
- VAPID_PRIVATE_KEY = 0UXtd842JGqP_0dM_blKj6MPG7OVoqRA4Z-SXPzHszg
- VAPID_EMAIL = mailto:admin@caddypro.kr
- NEXT_PUBLIC_VAPID_PUBLIC_KEY = BF1vEDu0zwtRRLvXmLBK4_08VwIQ5rwr6dmJpjQgLBGpnOaJSAbsCy4UW3CKEBJplxc_mfmUz0jpUUgMmwrxsvI
- CRON_SECRET = caddy_cron_secret_2025

### 빌드 결과
- Compiled successfully
- git push new_origin master -> c60c346

---

## ✅ 41회차 — Web Push 포기 → 카카오톡 채널 연동 UI 교체

> 날짜: 2026년 4월 7일
> 커밋: `f0f44d5`

### 배경
- Android Chrome에서 Web Push 반복 실패 (SW 활성화 시간 초과, no active Service Worker)
- 근본 원인: `next-pwa`가 빌드마다 `worker-{hash}.js` 파일명 변경 → 이전 캐시된 sw.js가 존재하지 않는 파일 참조 → SW 영구 설치 실패
- Web Push 완전 포기 → 카카오톡 채널 구독 유도로 방향 전환

### 변경 내용

**`src/app/settings/page.tsx`**
- push 관련 state 변수 모두 제거 (pushSupported, pushGranted, pushEnabled, pushLoading, pushMsg)
- push handler 함수 3개 제거 (handleRequestPush, handleTestPush, handleDisablePush)
- push 알림 권한 UI 섹션 → 카카오톡 채널 추가 버튼으로 교체
- 카카오 채널 URL: `https://pf.kakao.com/_kQsVX/chat`

### 수정 파일
- `src/app/settings/page.tsx` — -257줄 (push 코드 제거)

### 빌드 결과
- ✅ Compiled successfully
- git push new_origin master -> `f0f44d5`

---

## ✅ 42회차 — 카카오 "나에게 보내기" 알림 자동화 구현

> 날짜: 2026년 4월 7일
> 커밋: `0e4d6c9`

### 목표
- 각 사용자(캐디)가 카카오 로그인 1회 연동
- Vercel Cron 매일 아침 6:30 KST 자동 실행
- 오늘 일정을 **자기 카카오톡으로** 자동 발송
- 프리미엄 기능으로 판매 예정

### 동작 흐름
```
사용자 → 설정 페이지 "카카오 로그인으로 연동" 버튼 클릭
         ↓ (카카오 OAuth 로그인)
         ↓ license_code + access_token → Supabase 저장
매일 06:30 KST (21:30 UTC) → Vercel Cron 실행
         ↓
/api/cron/alarm 호출
         ↓
Supabase에서 카카오 연동 사용자 전체 조회
         ↓
각 사용자의 오늘 일정 조회
         ↓
카카오 "나에게 보내기" API 호출
         ↓
사용자 카카오톡에 메시지 도착:

[캐디 매니저 알림] 오늘 일정 (2026-04-07)
• 08:00 [1부] 김○○ 고객
• 13:00 [2부] 박○○ 고객
총 2건의 일정이 있습니다.
```

### 신규/수정 파일

| 파일 | 내용 |
|------|------|
| `src/app/api/auth/kakao/callback/route.ts` | **신규** — OAuth 인가코드 → 토큰 교환 → Supabase 저장 |
| `src/app/api/cron/alarm/route.ts` | **수정** — Web Push → 카카오 나에게 보내기로 전면 교체 |
| `src/app/settings/page.tsx` | **수정** — 카카오 연동/해제 UI 추가 |
| `supabase_kakao_token.sql` | **신규** — 토큰 저장 테이블 생성 SQL |
| `vercel.json` | **수정** — Cron 재추가 `30 21 * * *` (매일 06:30 KST) |

### Supabase 테이블: `aone_pro_caddypro_kakao_tokens`
```sql
-- supabase_kakao_token.sql 실행 필요 (아직 미실행)
id              BIGSERIAL PRIMARY KEY
license_code    TEXT UNIQUE          -- 이용권 코드 (외래키 역할)
access_token    TEXT                 -- 카카오 액세스 토큰
refresh_token   TEXT                 -- 갱신 토큰 (만료 시 자동 갱신)
expires_at      TIMESTAMPTZ          -- 토큰 만료 시각
created_at / updated_at
```

### Vercel 환경변수 (등록 완료)
```
KAKAO_REST_API_KEY          = bd875afa30bbddea5939e8dc62789c77
NEXT_PUBLIC_KAKAO_REST_API_KEY = bd875afa30bbddea5939e8dc62789c77
NEXT_PUBLIC_SITE_URL        = https://caddy-pink.vercel.app
```

### 카카오 개발자 콘솔 설정 (앱 ID: 1416818)
- 앱 이름: 내 시크릿 앱
- REST API 키: `bd875afa30bbddea5939e8dc62789c77`

**완료된 설정:**
- ✅ 카카오 로그인 활성화 ON
- ✅ 동의항목 > 카카오톡 메시지 전송 (talk_message) → 선택 동의 설정
- ✅ Redirect URI 등록: `https://caddy-pink.vercel.app/api/auth/kakao/callback`
  - 위치: 앱 > 플랫폼 키 > Default Rest API Key > ⋮ 클릭 > "카카오 로그인 리다이렉트 URI" 입력칸에 추가

**⚠️ 아직 미완료 (다음에 해야 할 것):**
1. **Supabase SQL 실행**
   - https://lajjbrrysvkaxzrchanp.supabase.co → SQL Editor
   - `supabase_kakao_token.sql` 전체 붙여넣고 실행
2. **테스트**
   - 앱 설정 페이지 → "카카오 로그인으로 연동" 버튼 클릭
   - 카카오 로그인 완료 → "카카오톡 연동 완료" 표시 확인
   - 수동 테스트: `https://caddy-pink.vercel.app/api/cron/alarm` GET 요청 (Authorization: Bearer caddy_cron_secret_2025)

### 빌드 결과
- ✅ Compiled successfully
- git push new_origin master -> `0e4d6c9`

### Vercel Cron 스케줄
```
30 21 * * *  →  매일 UTC 21:30 = KST 06:30
```
(Vercel Hobby 플랜: 하루 1회 cron 가능)

---

## ✅ 43회차 — 알리고 SMS 연동 (카카오 → SMS 전환)

> 날짜: 2026년 4월 8일  
> 커밋: `f868499`

### 배경
- 카카오 KOE101 오류 → 해결 불가 판단 → 알리고 SMS로 전면 전환

### 알리고 계정
| 항목 | 값 |
|------|-----|
| API KEY | `yczi82961yse34dwljg0wzk82n2h1v7d` |
| User ID | `supersm3` |
| 발신번호 | `01027377229` |

### 신규/수정 파일
| 파일 | 내용 |
|------|------|
| `src/lib/aligo.ts` | **신규** — sendSMS, buildWelcomeMsg, buildScheduleMsg |
| `src/app/api/cron/alarm/route.ts` | **수정** — 카카오 완전 제거, 알리고 SMS 발송으로 교체 |
| `src/app/api/notify/welcome/route.ts` | **신규** — 가입 환영 SMS API |

### Vercel 환경변수 (등록 완료)
```
ALIGO_API_KEY = yczi82961yse34dwljg0wzk82n2h1v7d
ALIGO_USER_ID = supersm3
ALIGO_SENDER  = 01027377229
```

### 빌드 결과
- ✅ Compiled successfully
- git push new_origin master → `f868499`

---

## ✅ 44회차 — SMS 알림 설정 UI + DB 컬럼 추가

> 날짜: 2026년 4월 8일  
> 커밋: `e5d0860`

### 변경 내용
1. **DB 컬럼 추가** — `aone_pro_caddypro_licenses` 테이블
   - `phone TEXT` — 알림 받을 전화번호
   - `notification_hour INT DEFAULT 6` — 매일 SMS 발송 시각 (5~9)

2. **API 신규** — `/api/notify/settings`
   - `GET ?licenseCode=XXX` → `{ phone, notification_hour }` 조회
   - `PUT { licenseCode, phone, notificationHour }` → DB 저장

3. **설정 페이지 UI 전면 교체**
   - 카카오 로그인 연동 UI 완전 제거
   - 전화번호 입력 (자동 하이픈 포맷팅 010-XXXX-XXXX)
   - 발송 시각 선택 버튼 (5~9시, 오렌지 테마)
   - "알림 설정 저장" 버튼 → `/api/notify/settings` PUT 호출

### 신규/수정 파일
| 파일 | 내용 |
|------|------|
| `src/app/api/notify/settings/route.ts` | **신규** — GET/PUT notify settings 엔드포인트 |
| `src/app/settings/page.tsx` | **수정** — 카카오 UI → 알리고 SMS 설정 UI 교체 |

### 빌드 결과
- ✅ Compiled successfully
- git push new_origin master → `e5d0860`

---

## ✅ 45회차 — 로컬 Chrome 캐시 초기화 배치 파일 추가

> 날짜: 2026년 4월 8일  
> 타입: 유틸리티 스크립트

### 목표
- 로컬 서버에서 이전 정적 파일/서비스워커 캐시가 남아 최신 화면이 안 뜨는 문제를 1회 실행으로 정리
- 포트번호를 받아 해당 localhost URL을 즉시 재오픈

### 신규 파일
| 파일 | 내용 |
|------|------|
| `clear-chrome-cache-port.bat` | 포트 입력(또는 인자) → Chrome 종료 → 프로필 캐시 삭제(Cache/Code Cache/GPUCache/Service Worker CacheStorage) → localhost 재오픈 |

### 사용 방법
```bat
clear-chrome-cache-port.bat 4455
```
또는 더블클릭 후 포트 입력.

- 실행 프롬프트 문구 개선: `캐시를 삭제할 포트번호를 입력하세요 (예: 4455)`
- 한글 깨짐 수정: 배치 실행 시 `chcp 65001` 적용 (UTF-8 코드페이지 강제)

### 참고
- Chrome이 켜진 상태에서는 캐시 파일이 잠겨 삭제가 안 될 수 있어 스크립트에서 먼저 `chrome.exe`를 종료함
- 마지막에 `?cache_bust=랜덤값`으로 URL을 열어 즉시 최신 리소스 요청 유도

---

## ✅ 46회차 — localhost 전용 초기화 + 문자알림 서버 리셋 유틸 추가

> 날짜: 2026년 4월 8일  
> 타입: 유틸리티 스크립트

### 배경
- 기존 캐시 삭제 스크립트는 `chrome.exe` 전체 종료 방식이라 열려 있던 탭이 모두 닫힘
- 문자알림 설정은 브라우저 캐시가 아니라 DB 값(`phone`, `notification_hour`)이라 캐시 삭제만으로 초기화되지 않음

### 신규 파일
| 파일 | 내용 |
|------|------|
| `clean-localhost-only.bat` | Chrome 기존 창을 닫지 않고, localhost 전용 임시 프로필로 새 창 실행 (격리된 깨끗한 상태) |
| `reset-sms-settings-local.bat` | 로컬 서버의 `/api/notify/settings`로 `phone=null`, `notificationHour=6` 리셋 요청 |

### 수정 파일
| 파일 | 내용 |
|------|------|
| `clear-chrome-cache-port.bat` | 실행 시 `chrome.exe` 전체 종료 경고 문구 추가 |

### 사용 가이드
1) 탭 안 닫고 localhost만 깨끗하게 보기:  
`clean-localhost-only.bat 4455`

2) 문자알림 서버값까지 초기화(license code 필요):  
`reset-sms-settings-local.bat 4455`

---

## ✅ 47회차 — 알리고 알림톡 템플릿 5종 초안 작성

> 날짜: 2026년 4월 8일  
> 타입: 기획/운영 문서

### 작성 목적
- 알리고 알림톡 승인 등록에 바로 사용할 템플릿 문구 사전 준비
- 가입/일정/개인일정/만료/연장 이벤트별 문구와 변수 정의 통일

### 신규 파일
| 파일 | 내용 |
|------|------|
| `알리고_알림톡_템플릿_초안.txt` | 알림톡 템플릿 5종(가입/오늘일정/개인일정/만료7일전/기간연장) + 변수 가이드 |

### 템플릿 구성
1. 가입완료_이용코드안내
2. 오늘일정_오전브리핑
3. 개인일정_사전알림
4. 이용권만료_D7
5. 이용권연장_완료안내

---

## 📋 다음 작업 (현재 미완료)

- [ ] **Cron 테스트**: 일정 등록 후 설정한 시각에 SMS 수신 확인
- [ ] **알림톡 연동**: 템플릿 심사 승인 후 Template ID 수집 → aligo.ts 알림톡 발송 함수 교체
- [ ] 딜러 정산 요청 알림 (알리고 SMS or 이메일)

---

## ✅ 54회차 — 관리자/딜러 발급 후 문자 미전송 근본 원인 수정

> 날짜: 2026년 4월 8일  
> 타입: 버그 수정  
> 커밋: `108cdfa`

### 문제 요약
- 관리자에서 코드 발급은 성공하지만 가입 안내 문자가 수신되지 않음
- 스크린샷으로 "알리고 환경변수 미설정" 경고 확인

### 근본 원인
`licenseUtils.ts`는 admin/dealer 클라이언트 컴포넌트에서 호출 → **브라우저에서 실행** → `process.env.ALIGO_API_KEY = undefined`  
⟹ `sendSMS()` 직접 호출 방식은 브라우저에서 절대 작동 불가

### 최종 수정 내용
- `issueVoucher()` 문자 발송 경로 변경
  - 기존(버그): `sendSMS()` 직접 호출 (브라우저에서 환경변수 없음)
  - 변경(정상): `fetch('/api/notify/welcome', {...})` → 서버 라우트에서 `sendSMS()` 호출 + 응답 await
- `extendLicense()`도 동일 패턴: `fetch('/api/notify/extend')` + 응답 await
- 결과에 따라 `smsOk: false` + `smsMessage` 반환 → 화면에 실패 사유 alert 표시

### 55회차 이전 수정 내역 (배포됨 `0518093`)
- `aligo.ts` 수신번호 정규화 (`receiver.replace(/\D/g, '')`)
- admin/딜러 페이지 발급화면에 `result.smsOk === false` 시 alert 추가

### 수정 파일
| 파일 | 변경 |
|------|------|
| `src/lib/licenseUtils.ts` | 발급/연장 SMS를 내부 API 라우트 경유로 수정 (응답 await + smsOk 반환) |

### 올바른 아키텍처
```
브라우저 → issueVoucher() → fetch('/api/notify/welcome') → [서버 Route] → sendSMS() → 알리고
```

---

## ✅ 53회차 — 개인일정 사전알림 시간 선택 UI + 사용자별 Cron 연동

> 날짜: 2026년 4월 8일  
> 타입: 기능 구현  
> 커밋: `f3ead03`

### 작업 목적
- 사용자마다 개인일정 알림을 "몇 분 전"에 받을지 직접 선택할 수 있도록
- 기존 Cron에 하드코딩된 1시간 → 사용자별 설정값(remind_before_min)으로 동적 처리

### 수정 내용

#### DB 마이그레이션
- `aone_pro_caddypro_licenses` 테이블에 `remind_before_min INTEGER DEFAULT 60` 컬럼 추가

#### settings API (`/api/notify/settings`)
- GET: `remind_before_min` 컬럼 SELECT 추가, 기본값 60 반환
- PUT: `remindBeforeMin` 파라미터 → DB 업데이트

#### Cron 로직 재설계 (`/api/cron/personal-remind-1h`)
- 기존: 단일 target(kstNow+60분)으로 모든 사용자 조회
- 변경: 전화번호 + remind_before_min(>0) 설정된 사용자 루프 → 각자 target time 계산 → 해당 시각 개인일정 조회 → 알림 발송
- `remindLabel` 파라미터 추가 — 알림 메시지에 "30분 전", "1시간 전" 등 동적 표시

#### settings UI (`/settings`)
- `remindBeforeMin` state 추가 (기본 60)
- useEffect에서 `d.remind_before_min` 로드
- 저장 시 `remindBeforeMin` 포함
- 알림 카드를 2섹션으로 분리:
  - **1번 섹션**: 매일 오전 브리핑 시각 (5~9시 선택)
  - **2번 섹션**: 개인일정 사전 알림 시간 (30분 전 / 1시간 전 / 2시간 전 / 안받기)

#### aligo.ts
- `buildPersonalEvent1hMsg()` — `remindLabel` 선택 파라미터 추가 (미제공 시 "1시간" fallback)

### 수정 파일
| 파일 | 변경 |
|------|------|
| `src/app/api/cron/personal-remind-1h/route.ts` | 하드코딩 60분 → 사용자별 remind_before_min 기반 동적 처리 |
| `src/app/api/notify/settings/route.ts` | remind_before_min GET/PUT |
| `src/app/settings/page.tsx` | remindBeforeMin state, 2섹션 UI, 전화번호 readonly 모드 |
| `src/lib/aligo.ts` | buildPersonalEvent1hMsg remindLabel 파라미터 추가 |
| DB | `remind_before_min` 컬럼 추가 |

---

## ✅ 52회차 — 개인일정 1시간 전 자동 알림 Cron 구현

> 날짜: 2026년 4월 8일  
> 타입: 기능 구현

### 작업 목적
- 개인일정을 "시작 1시간 전"에 자동으로 알림 발송
- 아침 1회 브리핑과 별도로, 일정 시간 기준 리마인드 제공

### 수정 내용
- 신규 API 추가: `/api/cron/personal-remind-1h`
  - 5분 간격 호출을 전제로, KST 기준 "정확히 1시간 후" 개인일정 조회
  - 대상: `type='personal'`, `date=targetDate`, `time/start_time=targetHM`
  - 전화번호가 등록된 이용권만 발송
- 메시지 빌더 추가: `buildPersonalEvent1hMsg()`
- Vercel Cron 추가: `"*/5 * * * *"` → `/api/cron/personal-remind-1h`

### 수정 파일
| 파일 | 변경 |
|------|------|
| `src/app/api/cron/personal-remind-1h/route.ts` | 신규 생성 — 1시간 전 개인일정 알림 발송 |
| `src/lib/aligo.ts` | `buildPersonalEvent1hMsg()` 추가 |
| `vercel.json` | 5분 주기 personal reminder cron 추가 |

### 운영 참고
- Vercel Hobby 플랜 Cron 제한이 있어 전체 Cron이 모두 실행되지 않을 수 있음
- 안정 운영 시 Vercel Pro 또는 외부 스케줄러(Supabase/Cloudflare) 검토 필요

---

## ✅ 51회차 — 관리자 발급 코드 prefix 및 문자 미발송 보완

> 날짜: 2026년 4월 8일  
> 타입: 버그 수정

### 문제 요약
- 관리자 직접 발급 코드가 `DC-`로 시작해 딜러 발급 코드로 오해될 수 있음
- 관리자 발급 폼에서 연락처가 비어 있어도 발급되어 가입 완료 문자가 미발송되는 사례 발생

### 수정 내용
- 관리자 직접 발급 prefix를 `DC` → `AD`로 변경
- 관리자 발급 시 연락처 미입력이면 발급 중단 + 경고 알림
- 발급 INSERT 시 `phone` 컬럼에도 연락처 동시 저장 (`user_phone`과 동기화)

### 수정 파일
| 파일 | 변경 |
|------|------|
| `src/lib/licenseUtils.ts` | `CHANNEL_PREFIX.direct`를 `AD`로 변경, INSERT에 `phone` 컬럼 저장 추가 |
| `src/app/admin/page.tsx` | `handleIssue()`에 연락처 필수 검증 추가, 라벨을 `연락처 (필수: 문자 발송)`으로 변경 |

### 기대 효과
- 관리자 발급 코드와 딜러 발급 코드 식별 명확화
- 발급 직후 환영 문자 미발송 케이스 감소
- 일정 알림용 전화번호 데이터 누락 방지

---

## ✅ 50회차 — 설정 화면 문구/아이콘 개선

> 날짜: 2026년 4월 8일  
> 타입: UI 개선

### 변경 내용
- 설정 화면 제목을 `문자 알림 설정` → `카카오톡/문자 알림 설정`으로 변경
- `카카오톡` 앞에 말풍선 아이콘(`MessageCircle`) 추가
- 안내 박스에 `오늘 일정이 없으면 알림이 발송되지 않습니다.` 문구 추가

### 수정 파일
| 파일 | 변경 |
|------|------|
| `src/app/settings/page.tsx` | 알림 섹션 제목/아이콘/안내문구 수정 |

### 동작 기준
- 오늘 일정(`aone_pro_caddypro_schedules`)이 0건이면 오전 브리핑 SMS는 발송되지 않음
- 개인일정 알림도 대상 일정이 없으면 발송되지 않음

---

## ✅ 49회차 — 알리고 SMS 5개 템플릿 전체 구현 완료

> 날짜: 2026년 4월 8일  
> 타입: 기능 구현

### 구현 완료 5개 템플릿 (일반 SMS/LMS — 알림톡 심사 중)

| # | 템플릿명 | 트리거 | 구현 위치 |
|---|---------|--------|---------|
| 1 | 가입완료_이용코드안내 | 이용권 발급 시 | `licenseUtils.ts` → `issueVoucher()` → `/api/notify/welcome` |
| 2 | 오늘일정_오전브리핑 | 매일 오전 지정 시각 | `/api/cron/alarm` → `buildScheduleMsg()` |
| 3 | 개인일정_사전알림 | 오전 브리핑 시 개인일정 별도 발송 | `/api/cron/alarm` → `buildPersonalEventMsg()` |
| 4 | 이용권만료_D7/3/1 | 매일 KST 10시 | `/api/cron/expire-notify` → `buildExpireMsg()` |
| 5 | 이용권연장_완료안내 | 연장 완료 시 | `licenseUtils.ts` → `extendLicense()` → `/api/notify/extend` |

### 신규/수정 파일

| 파일 | 변경 |
|------|------|
| `src/lib/aligo.ts` | `buildPersonalEventMsg()`, `buildExtendMsg()` 추가 |
| `src/app/api/cron/alarm/route.ts` | 개인일정 전용 SMS 추가 발송 로직 (time 컬럼 포함) |
| `src/lib/licenseUtils.ts` | `extendLicense()` — `code, phone, tier` select 추가 + 연장완료 SMS fire-and-forget |
| `src/app/api/notify/extend/route.ts` | 신규 생성 — 연장완료 LMS 발송 API |

### 템플릿 3 (개인일정 사전알림) 동작 방식
- 별도 고빈도 Cron 불필요 (Vercel Hobby limit)
- 오전 브리핑 Cron이 실행될 때, 오늘 개인일정이 있으면 일정별 전용 SMS 추가 발송
- 시간(`time` or `start_time`) 있는 개인일정만 대상

---

## ✅ 48회차 — 알리고 SMS 전체 기능 구현 완료 (템플릿 심사 중 우선 SMS)

> 날짜: 2026년 4월 8일  
> 타입: 기능 구현

### 작업 내용
알림톡 템플릿 심사 대기 중이지만, 일반 SMS(LMS)로 모든 알림 기능을 선행 구현.  
템플릿 승인 후 알림톡 API로 교체 예정.

### 구현 항목

| 기능 | 파일 | 설명 |
|------|------|------|
| 가입 시 환영 SMS 자동 발송 | `src/lib/licenseUtils.ts` | `issueVoucher()` INSERT 성공 후 `/api/notify/welcome` fire-and-forget fetch 추가 |
| 만료 D-7/D-3/D-1 알림 Cron | `src/app/api/cron/expire-notify/route.ts` | 매일 KST 10시 실행, d-7/3/1에만 발송 |
| 만료 알림 메시지 빌더 | `src/lib/aligo.ts` | `buildExpireMsg()` 함수 추가 |
| Vercel Cron 등록 | `vercel.json` | `/api/cron/expire-notify` `0 1 * * *` (UTC 01:00 = KST 10:00) 추가 |

### 신규/수정 파일

| 파일 | 변경 |
|------|------|
| `src/lib/licenseUtils.ts` | `issueVoucher()` — INSERT 성공 후 userPhone 있으면 `/api/notify/welcome` fetch |
| `src/lib/aligo.ts` | `buildExpireMsg()` 함수 추가 |
| `src/app/api/cron/expire-notify/route.ts` | 신규 생성 — 만료 D-7/3/1 SMS Cron |
| `vercel.json` | expire-notify cron 추가 (`0 1 * * *`) |

### 설정 페이지 전화번호 자동 불러오기
- 이미 구현 완료 상태 (47회차 이전)
- `settings/page.tsx` useEffect → `GET /api/notify/settings?licenseCode=XXX` → phone/notification_hour 자동 세팅

### 만료 Cron 로직
- 매일 UTC 01:00 (KST 10:00) 실행
- `expires_at BETWEEN NOW() AND NOW()+7days` 쿼리
- daysLeft가 **7, 3, 1일**인 경우에만 SMS 발송 (중복 방지)

### 알림톡 템플릿 현황 (심사 중)
| 템플릿명 | 상태 |
|---------|------|
| 가입완료_이용코드안내 | 심사중 |
| 오늘일정_오전브리핑 | 심사중 |
| 개인일정_사전알림 | 심사중 |
| 이용권만료_D7 | 심사중 |
| 이용권연장_완료안내 | 심사중 |


